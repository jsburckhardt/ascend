// Placeholder module so the application-source directory is tracked and the
// TypeScript toolchain has an input. Contains no application logic.
// Application code is added by later Prototype 0 stories (see ADR-0002).
export {};
