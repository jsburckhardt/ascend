# Research Brief: BL-003: Select a viable browser workbench presentation

## GitHub Issue
- **Issue:** #9
- **Title:** BL-003: Select a viable browser workbench presentation
- **Work Item:** project/work-items/9-bl-003-select-a-viable-browser-workbench-presentation

## Scope Classification
- **Scope Type:** architecture_decision

## Problem Statement
Ascend has not selected how code-server will be presented inside the two-surface MVP. Issue #9 requires a bounded comparison of only an embedded workbench and a full-page workbench with a minimal Ascend header on the designated desktop-Chromium host. The comparison must preserve the merged BL-001 lifecycle and fixture guarantees and the merged BL-002 terminal-parity guarantees while observing browser-policy, framing, origin, resource, clipboard, WebSocket, keyboard, Preview, cleanup, and integrity behavior. Project Home, stable routing or proxy integration, runtime management, lifecycle UI, polished UI, and tablet acceptance remain outside this issue.

## Acceptance Criteria
**Core**
- [ ] Exactly two candidates are compared: (1) code-server embedded in an Ascend surface and (2) full-page code-server with a minimal Ascend header; no third presentation enters the comparison.
- [ ] Before any attempt starts, prerequisites are checked in this exact order: Ubuntu 24.04, non-root `vscode` user, merged BL-001/BL-002 proof capabilities and canonical fixture, code-server 4.131.0, repository-declared Chromium desktop build, and creation of a 1440 by 900 viewport; the first failed check determines the prerequisite stop reason.
- [ ] Both candidates use the same BL-001 canonical fixture, code-server configuration, Chromium executable/version, and 1440 by 900 viewport for every attempt; presentation is the only candidate-specific variable.
- [ ] Every attempt executes the same fixed scenario once in this order: navigate the candidate and receive a final document HTTP status from 200 through 399; find the BL-001 Explorer sentinel; open the known Markdown fixture; observe its rendered Preview sentinel in the editor webview; use keyboard-only workbench actions to focus Explorer, enter and leave the Preview, and open one integrated terminal; complete the clipboard step; then run the BL-002 identity, canonical-path, and exact fixed tool commands with their existing parity assertions.
- [ ] The clipboard step creates a unique per-attempt token only in memory, types it into unexecuted terminal input, copies it, clears the input, pastes it, and observes the exact token in terminal input; the token is never executed or written to the fixture.
- [ ] Fixture integrity is independently proven for every attempt by exact before/after comparison of the complete BL-001 fixture tree membership and every sentinel byte sequence defined by the merged BL-001 proof.
- [ ] Every attempt captures all document and subresource HTTP responses, request failures, console warnings/errors, page errors, and WebSocket open/error/close events; at least one workbench WebSocket opens before workbench interaction and has no error or unexpected close before the terminal commands complete.
- [ ] Browser events are counted deterministically with blocking precedence: a blocking browser protocol violation is each browser-reported blocked, refused, denied, or policy-enforced failure involving frame navigation, CSP/frame-ancestors/X-Frame-Options, origin/CORS, mixed content, sandbox/permission, cookie/storage access, a required workbench or Preview resource, WebSocket handshake/transport, or a WebSocket close before terminal completion; a non-blocking warning is each console warning/error, HTTP status of 400 or greater, request failure, or WebSocket warning/close that is not blocking and does not prevent a functional assertion. Every occurrence meeting either definition is retained once, blocking occurrences are excluded from the non-blocking count, and repeated occurrences are counted separately.
- [ ] A functional assertion fails when its required observable outcome is absent, including document navigation, Explorer, Preview webview, keyboard focus/action, clipboard, terminal parity, or required WebSocket usability; browser events that do not prevent those outcomes remain retained for ordered comparison.
- [ ] An attempt is fresh and independently started only when it has a new BL-001 process handle and process group, a new browser context with empty per-attempt browser state, a new candidate-disposable area, and no process, context, terminal command, listener, or disposable state retained from a prior attempt.
- [ ] A unique run ID is recorded before candidate navigation; each started attempt runs the fixed scenario at most once, no failed assertion, transient error, or timeout is retried, and every started attempt is retained whether it passes or fails.
- [ ] Each started attempt retains one machine-readable record with artifact references for candidate, slot, run ID, exact Chromium version, start status `started`, final status `passed` or `failed`, all failed assertion identifiers and errors, monotonic navigation-start and scenario-completion milliseconds when reached, every functional/evidence/cleanup/integrity assertion, raw browser-event evidence, both warning counts, exact cleanup results, and fixture-integrity results.
- [ ] Cleanup is attempted after every started attempt and passes only when the browser context is closed, every terminal command started by the attempt is absent, the exact BL-001 process group, PID, and listener are absent, only that attempt disposable area is removed, and the fixture remains present and unchanged.
- [ ] Each candidate is eligible only when all three of its independently started fresh no-retry attempts have final status `passed`, with every functional, required-evidence, cleanup, and integrity assertion passing; a missing required artifact or cleanup failure makes the attempt fail and candidate ineligible.
- [ ] One comparison record contains exactly six ordered slots — embedded 1 through 3, then full-page 1 through 3 — and records Ubuntu version, runtime hostname, user, Chromium name/version, code-server version, viewport, candidate eligibility, total retained counts for both warning categories, all three elapsed navigation-to-scenario-completion values and their middle-value median for each eligible candidate, final disposition, and references to every started attempt.

**Edge Cases**
- [ ] Cleanup failure stops execution before any later slot starts; selection is still evaluated from completed eligibility evidence, so one already-eligible candidate is selected, while zero eligible candidates produce `no viable candidate`.
- [ ] If execution stops before all six attempts start, every remaining slot is `not started` and has no run ID; the comparison records exactly one stop reason as either `prerequisite failure:<first-failed-prerequisite>` or `cleanup failure:<candidate>/<attempt>`, and never fabricates a run ID.
- [ ] A prerequisite failure starts no attempt, makes both candidates ineligible, returns nonzero with exact disposition `no viable candidate`, and creates no Accepted ADR.
- [ ] If exactly one candidate is eligible, it is selected, the comparison exits zero, and the exact disposition is `embedded selected` or `full-page selected`.
- [ ] If both candidates are eligible, selection applies these tie-breakers in exact order: (1) fewer total retained blocking browser protocol violation occurrences across its three attempts, (2) fewer total retained non-blocking console/request/WebSocket warning occurrences across its three attempts, and (3) lower middle-value median of the three integer monotonic elapsed times from navigation start to fixed-scenario completion; the first strictly lower value selects the candidate.
- [ ] If both eligible candidates tie on all three ordered measures, the comparison returns nonzero with exact disposition `selection tie`, selects neither, and creates no Accepted ADR.
- [ ] If neither candidate is eligible, the comparison returns nonzero with exact disposition `no viable candidate`, selects neither, and creates no Accepted ADR.
- [ ] At completion, exactly one Accepted ADR for BL-003 exists only when a candidate was selected; it records desktop-Chromium authority, both candidates, the fixed scenario, comparison-evidence references, eligibility and ordered tie-breaker results, the selected candidate, the rejected alternative, and tablet validation as a separate non-authoritative follow-up, and repeating selection from the same comparison record creates no duplicate Accepted ADR.

**Verification**
- [ ] The designated-host comparison starts all six attempts when prerequisites and cleanup pass, retains the six attempt records plus comparison record, and exits with the disposition dictated by the eligibility and selection rules.
- [ ] Bounded repository validation proves the exact selector outcomes for embedded-only eligible, full-page-only eligible, each of the three ordered tie-breakers, `selection tie`, and `no viable candidate`.
- [ ] Bounded repository validation proves missing-artifact ineligibility, cleanup-failure stop and missing slots, each ordered prerequisite failure, no-retry behavior, absent fabricated run IDs, exact cleanup, clipboard-memory-only behavior, and separate fixture tree/sentinel-byte integrity; each case has a finite exit result and inspectable record.
- [ ] Documentation states the ordered prerequisites, exact two candidates, 1440 by 900 viewport, fixed scenario, fresh/no-retry attempt and eligibility rules, event-count rules, required attempt/comparison evidence, cleanup and integrity rules, stop reasons, tie-breaker order, four exact final dispositions, authoritative desktop Chromium result, and separate non-authoritative tablet follow-up, and its reported result matches the comparison record and any Accepted ADR.
- [ ] `just verify` exits zero.

## Repository Findings
- GitHub Issue #9 contains a structured acceptance-criteria block delimited by `ACCEPTANCE_CRITERIA_START` and `ACCEPTANCE_CRITERIA_END`. It has 28 ordered checkbox criteria: 15 Core, 8 Edge Cases, and 5 Verification criteria. The criteria are reproduced above without alteration.
- The active branch is `feat/9-select-browser-workbench-presentation` at `e87c5c7`, the same commit as `main` and `origin/main`. Git history identifies that commit as the merged BL-002 change and its parent `fa32a6f` as the merged BL-001 change. The working tree was clean before this brief was written.
- No `project/work-items/9-*` directory existed when path resolution ran. The title-derived stable path is therefore `project/work-items/9-bl-003-select-a-viable-browser-workbench-presentation` (`project/README.md`; `CORE-COMPONENT-260806-rpiv-stage-contract.md`).
- `PRD.md` DD-001 delegates editing, Explorer, Preview, terminal, Git UI, and extensions to code-server while Ascend owns navigation and lifecycle. DD-005 through DD-009 establish code-server as the initial provider, direct host execution, a host-native terminal, one runtime per active project, and Project Home plus Project Workbench as the two MVP surfaces. Slice 3 names the same two presentation candidates as Issue #9.
- `PRD.md` NFR-019 makes a modern Chromium desktop browser authoritative for primary workflows and treats tablet usability as a manual MVP evaluation. NFR-021 requires workbench routing to support code-server and integrated-terminal WebSocket traffic. A-001 assumes code-server adequately supplies Explorer, Markdown Preview, terminal, Git, and extensions; A-002 assumes host-process terminal parity; A-004 assumes acceptable keyboard, clipboard, terminal, and WebSocket behavior. The risk register specifically identifies poor iframe behavior and reverse-proxy breakage of WebSockets or editor webviews.
- The current product application does not expose either candidate. `apps/web/src/App.tsx::App` renders only a static Project Home header, Open Project button, and empty state. `apps/api/src/app.ts::app` only autoloads plugins and routes, and `apps/api/src/routes/root.ts` only returns API health. Tracked application source contains no iframe, reverse-proxy, upgrade-handler, framing-policy, or workbench-route implementation. `apps/web/src/App.test.tsx` and `tests/e2e/project-home.spec.ts` cover only the scaffold.
- The existing workbench path is a standalone proof, not an Ascend presentation. `apps/api/src/workbench-proof-runtime.ts::startWorkbenchProof` directly spawns `/home/vscode/.local/bin/code-server`, binds `127.0.0.1:0`, disables authentication, telemetry, update checks, and workspace trust, creates isolated user/extension data below a run directory, and supplies the canonical fixture as one argument. `tests/e2e/workbench-proof.spec.ts` navigates the emitted loopback URL directly.
- `apps/api/src/workbench-proof-contract.ts` defines the canonical fixture as `tests/fixtures/bl-001/workbench project;BL-001`, the Explorer sentinel as `EXPLORER-SENTINEL-BL-001.txt`, the Markdown fixture as `WORKBENCH-PREVIEW.md`, and the rendered sentinel as `BL-001 Markdown Preview Rendered Sentinel`. `snapshotFixture` records complete recursive membership and SHA-256 hashes for both sentinel files. `apps/api/test/workbench-proof-contract.test.ts` pins the four-member fixture tree and the two sentinel hashes.
- The BL-001 runtime contract provides a UUID run ID, a versioned PID/URL/start-time handle, 200-through-399 readiness, a dedicated process group, exact identity checks, bounded SIGTERM/SIGKILL cleanup, loopback listener/process audits, and per-run disposal (`apps/api/src/workbench-proof-runtime.ts`; `apps/api/src/workbench-proof-audit.ts`). Its current run directory is removed by `stopWorkbenchProof`; `tests/e2e/workbench-proof.spec.ts::removeAbsentPriorRuns` removes the complete generated `test-results/bl-001` root after refusing to remove a live prior handle.
- BL-001 tests cover canonical metacharacter-path handling, exact-handle stop and repeated stop, five named startup failures, fixture integrity, process ownership, loopback attribution, unrelated-process survival, and escalation (`apps/api/test/workbench-proof-contract.test.ts`, `workbench-proof-runtime.test.ts`, `workbench-proof-failures.test.ts`, and `workbench-proof-environment.test.ts`). The merged implementation and independent verification records report acceptance under `project/work-items/5-bl-001-prove-a-host-code-server-workbench/`.
- BL-002 extends the same proof rather than adding an application API. `apps/api/src/workbench-proof-contract.ts` fixes identity probes `hostname`, `id -un`, and `pwd -P`, followed by exactly `git --version`, `git status --short`, `gh --version`, `tmux -V`, `docker --version`, and `copilot --version`. It fixes 5,000 ms per command and 90,000 ms for the terminal episode and permits only line-ending normalization.
- `apps/api/src/workbench-proof-terminal.ts` directly spawns each command with argument arrays, records exact process identities, retains separate raw and normalized streams, atomically writes raw records, and classifies only `PATH`. `apps/api/src/workbench-proof-terminal-episode.ts::runTerminalParityEpisode` preflights tools, owns one browser context and workbench handle, and cleans tracked commands, browser context, workbench group, PID, and listener in order while preserving operation and cleanup failures.
- The current passing E2E captures the direct terminal context before starting code-server, then opens one integrated terminal after Explorer and Preview interaction. Integrated evidence is written to fixed global paths under `test-results/bl-001/terminal-parity`, so later runs replace the same generated files. The scenario does not exercise clipboard copying, keyboard entry/exit of Preview, either Issue #9 presentation, browser-event classification, WebSocket lifecycle capture, six ordered slots, eligibility, selection, or ADR creation (`tests/e2e/workbench-proof.spec.ts`).
- The existing Preview helper refreshes detached webview frames inside a bounded poll because code-server replaces the frame during Preview initialization. The BL-001 implementation and verification records identify that narrow retry as necessary for stable observation. The Playwright configuration separately uses `trace: on-first-retry`, while Issue #9 requires each started comparison attempt to run once without retry (`tests/e2e/workbench-proof.spec.ts::renderedPreviewIsVisible`; `playwright.config.ts`).
- Existing generated BL-002 evidence at `test-results/bl-001/terminal-parity/episode.json` reports Ubuntu 24.04.4 LTS, host `03f809395a5d`, non-root `vscode` uid 1000, code-server 4.131.0, a passed browser observation, parity for all six fixed tools, an allowed `PATH` difference with identical executable resolutions, equal fixture snapshots, and successful command/context/handle/listener cleanup. Direct and integrated raw records each contain all nine commands with 5,000 ms bounds and exact-process absence. `.gitignore` excludes all `test-results/`; retained BL-002 records are under `project/work-items/7-bl-002-prove-host-native-terminal-parity/`.
- Current host inspection matches the designated environment: Ubuntu 24.04.4 LTS on x86_64, hostname `03f809395a5d`, user `vscode` uid 1000, and `/home/vscode/.local/bin/code-server` 4.131.0 with Code 1.131.0. `.devcontainer/devcontainer.json` requests code-server 4.131.0, Node 22, pnpm 10.34.5, the non-root `vscode` user, and Docker/tmux/GitHub/Copilot tooling; `.devcontainer/Dockerfile` installs `wl-clipboard`.
- `just setup` installs Playwright Chromium, `playwright.config.ts` declares one `chromium` project using Playwright `Desktop Chrome`, and `pnpm-lock.yaml` locks Playwright 1.62.1. Current repository-installed Chromium resolves to `/home/vscode/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome` and reports 151.0.7922.34. The declared Desktop Chrome device defaults to a 1280 by 720 viewport; no tracked Issue #9 1440 by 900 viewport declaration exists.
- Installed code-server help exposes proxy, trusted-origin, socket, proxy-domain, and absolute proxy-base-path options. BL-001 launch arguments do not configure those options, and no tracked source or documentation establishes headers, origins, cookie/storage policy, framing behavior, candidate URLs, or WebSocket routing for either candidate.
- The root `justfile` remains the command interface. `just verify` runs format, lint, typecheck, unit tests, builds, and all Playwright E2E tests; `just proof-terminal-parity` runs the existing workbench proof spec. `.harness/extensions/checks/extension.ts` wraps `just verify` with a 120,000 ms bound, and `.harness/extensions/boot/extension.ts` wraps checks with a 150,000 ms bound. `.harness/engineering-harness.md` keeps boot test-backed and non-persistent, lists the two existing real Chromium scenarios, and identifies live API/web startup and architecture checks beyond BL-001 as gaps.
- `project/architecture/ADR/DECISION-LOG.md` registers two Accepted ADRs and no BL-003 presentation decision. There is no existing Accepted ADR for the embedded-versus-full-page selection.

## Constraints
- Comparison scope is exactly the two candidates and six ordered slots stated by Issue #9. Project Home, stable routing/proxy integration, a general runtime manager, lifecycle UI, polished UI, and tablet acceptance are excluded.
- Desktop Chromium is authoritative (`PRD.md` NFR-019). Tablet validation is separate and non-authoritative.
- Every attempt must use the same merged BL-001 fixture, fixed code-server version/configuration, repository-declared Chromium executable/version, and 1440 by 900 viewport. Existing Playwright defaults are 1280 by 720.
- BL-001 safety boundaries remain applicable: canonical readable path, argument-array launch, non-root host execution, loopback binding, isolated run state, exact process identity/group ownership, bounded cleanup, no unrelated process/listener sweep, and complete fixture membership plus sentinel-byte preservation (`CORE-COMPONENT-260808-filesystem-path-safety.md`; `CORE-COMPONENT-260808-host-process-environment.md`; `CORE-COMPONENT-260808-runtime-lifecycle-error-handling.md`).
- Existing stop behavior removes the exact BL-001 run directory, and E2E preparation may remove the full generated BL-001 root only after proving prior handles absent. Issue #9 additionally requires retained per-attempt records and candidate-disposable isolation; those records do not exist in the current contract.
- Merged BL-002 identity, canonical-path, command order, parity assertions, 5,000 ms command bound, line-ending-only normalization, `PATH` policy, and exact tracked-command cleanup are fixed existing contracts (`workbench-proof-contract.ts`; `workbench-proof-terminal.ts`; `docs/workbench-proof.md`).
- Browser and runtime records must not become product telemetry containing source, terminal, command output, clipboard contents, prompts, credentials, or secrets (`PRD.md` NFR-010; `CORE-COMPONENT-260808-structured-runtime-logging.md`). Existing raw terminal output is confined to generated ignored proof artifacts.
- Ascend must continue delegating editor, Explorer, Preview, and terminal functionality to code-server (`ADR-260808-typescript-monorepo.md`; `PRD.md` DD-001 and NFR-012).
- The root `justfile` is canonical. Harness checks delegate to `just verify`, and harness boot remains non-persistent under the current Accepted harness ADR (`CORE-COMPONENT-260806-project-command-interface.md`; `ADR-260808-governed-engineering-harness.md`).
- Any Accepted ADR is global, follows `ADR-yymmdd-short-slug.md`, uses its UTC creation date, does not overwrite another artifact, and is registered by full ID in `project/architecture/ADR/DECISION-LOG.md` (`CORE-COMPONENT-260806-architecture-artifact-naming.md`). Issue #9 further constrains Accepted ADR existence to a selected candidate and forbids duplicates for the same comparison record.
- Research is limited to findings, constraints, relevant architecture, risks, and open questions. Selection, design, implementation tasks, validation design, and architecture-artifact naming belong to later RPIV stages (`CORE-COMPONENT-260806-rpiv-stage-contract.md`).

## Relevant ADRs and Core-Components
- `project/architecture/ADR/ADR-260808-typescript-monorepo.md` (Accepted): fixes the direct-host code-server stack, delegates IDE behavior to code-server, and identifies reverse-proxy behavior as requiring careful isolation and testing.
- `project/architecture/ADR/ADR-260808-governed-engineering-harness.md` (Accepted): keeps root recipes canonical and harness readiness non-persistent until a later decision defines live-service ownership, health, and cleanup.
- `CORE-COMPONENT-260808-host-process-environment.md` (Adopted): governs least-privilege host execution, canonical working directories, deterministic tools, argument arrays, loopback binding, and the stable-route product direction.
- `CORE-COMPONENT-260808-filesystem-path-safety.md` (Adopted): requires canonical paths and prohibits mutation of user-owned project directories.
- `CORE-COMPONENT-260808-runtime-lifecycle-error-handling.md` (Adopted): centralizes process ownership, requires explicit failures, and prohibits success-shaped fallbacks.
- `CORE-COMPONENT-260808-structured-runtime-logging.md` (Adopted): requires structured records and excludes sensitive development content from logs.
- `CORE-COMPONENT-260806-architecture-artifact-naming.md` (Adopted): governs global ADR IDs and decision-log registration.
- `CORE-COMPONENT-260806-project-command-interface.md` and `CORE-COMPONENT-260808-development-standards.md` (Adopted): make root recipes canonical and establish repository validation standards.
- `CORE-COMPONENT-260806-agent-executable-acceptance-criteria.md` and `CORE-COMPONENT-260806-rpiv-stage-contract.md` (Adopted): require bounded repository-executable criteria and preserve Research as a non-design handoff.
- `project/architecture/ADR/DECISION-LOG.md` is authoritative. Decisions 28 through 34 govern direct-host workbenches, delegated IDE behavior, runtime/logging/path/environment boundaries; decisions 35 through 42 govern the harness.

## Risks and Open Questions
- Neither candidate, its URL/origin relationship, nor its framing/header boundary exists in tracked application code. Frame-navigation, CSP, X-Frame-Options, origin/CORS, mixed-content, sandbox, permission, cookie/storage, resource, and WebSocket behavior is unobserved.
- It is unresolved what exact repository declaration identifies authoritative Chromium. The manifest uses a compatible range, the lockfile fixes Playwright 1.62.1, setup downloads Chromium, and the executable path includes a cache revision; current browser facts are runtime observations rather than a tracked comparison record.
- No repository artifact currently proves creation of the required 1440 by 900 viewport before navigation.
- Existing browser contexts do not capture the complete required browser-event set. No contract maps overlapping events to blocking precedence, excludes blocking events from non-blocking counts, or preserves repeated occurrences.
- Installed code-server has proxy and trusted-origin controls, but the current launch does not use them and stable proxy integration is excluded. Conditions under which both presentations vary only by presentation remain unspecified.
- Preview observation tolerates detached-frame replacement inside a poll, and Playwright tracing is configured for first retry. It is unresolved whether Issue #9 no-retry applies only to complete attempts or also changes these existing bounded semantics.
- The passing BL-002 episode performs direct capture before workbench start and uses fixed global raw-evidence paths. Compatibility with the required scenario order and six independently retained attempts is unresolved.
- Existing cleanup has a fallback direct stop after failed recipe stop. Whether that is compatible with no-retry and cleanup-stop semantics is unresolved.
- No current proof exercises keyboard-only Preview entry/exit or clipboard copy/paste. Clipboard behavior in current headless Chromium, terminal, and devcontainer display conditions is uncharacterized despite `wl-clipboard` installation.
- Generated evidence is ignored and may be deleted during preparation. No path or retention boundary exists for six attempt records, one comparison record, raw browser events, or conditional ADR references.
- Harness checks has a 120,000 ms deadline and boot has 150,000 ms. The current proof includes a 20,000 ms timeout scenario and a passing episode observed at 14,078 ms; compatibility with a six-attempt comparison duration is unknown.
- Cleanup failure can prevent later slots while eligibility requires three passes. No existing selector or record represents partial completed evidence; this exists only in Issue #9 criteria.
- No BL-003 ADR exists. The issue permits one only after selection and requires idempotence against the same comparison record; no existing comparison-record identity convention exists.
