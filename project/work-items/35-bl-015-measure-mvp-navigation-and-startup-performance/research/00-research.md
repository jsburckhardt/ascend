# Research Brief: BL-015: Measure MVP navigation and startup performance

## GitHub Issue
- **Issue:** #35
- **Title:** BL-015: Measure MVP navigation and startup performance
- **Work Item:** project/work-items/35-bl-015-measure-mvp-navigation-and-startup-performance

## Scope Classification
- **Scope Type:** issue

## Problem Statement
Ascend lacks one integrated-product, designated-host measurement record for NFR-001 warm navigation, NFR-002 cold startup, NFR-003 three-project capacity, and Product Metrics 2–4. Existing BL-004 and BL-010–014 artifacts prove separate raw-capacity, runtime, routing, navigation, isolation, and continuity boundaries, but their clocks, fixtures, endpoints, sequences, and retained artifacts do not constitute the fixed release measurement required by Issue #35. The research boundary is evidence, constraints, comparability, architecture, risks, and unresolved questions only.

## Acceptance Criteria
**Core**
- [ ] One paved command checks and records the repository-documented designated-host prerequisites before any attempt, returns a prerequisite-specific nonzero result with zero attempts when they are not met, and otherwise runs one plan in this exact order: five cold-start attempts, ten warm-reconnect attempts, three three-project continuity runs, and fresh capacity cohorts of 3, 5, and 10. The command has a documented finite overall bound, performs no automatic retries, and retains every planned attempt or cohort record.
- [ ] The retained measurement plan declares the host, fixtures, workload, project and section order, counts, timeouts, cache and prewarm rules, event definitions, clock precision and units, formulas, targets, and failure/disposition rules before the first attempt timestamp. Validation proves that every result uses that same declaration without post-run substitution.
- [ ] Each cold-start attempt measures from the controller-observed keyboard/user Open activation through separately defined runtime launch/readiness and stable-route document-readiness consequences to a workbench-usable consequence requiring both the expected Explorer sentinel and a ready terminal prompt. Each named consequence has a predeclared controller-observable event. Evidence retains every phase timestamp and duration plus total duration, and compares the required cold statistics with the unchanged 15,000 millisecond NFR-002 and Metric 4 target.
- [ ] The immutable pre-run plan fixes the exact project sequence for all five cold-start attempts; evidence matches that sequence without substitution. The attempts use no hidden prewarming, record browser context and cache state, and prove all run-attributed runtime resources absent before the next attempt.
- [ ] Each warm-reconnect attempt begins only after the existing health and readiness checks pass for a running runtime and measures from the controller-observed Open activation to the same workbench-usable consequence. Runtime PID, process-start identity, and port token remain unchanged. Evidence retains every phase and total and compares the required warm statistics with the unchanged 2,000 millisecond NFR-001 and Metric 3 target.
- [ ] The warm run contains exactly ten attempts in project order `A/B/C/A/B/C/A/B/C/A`, returns to Home between attempts, and never stops or replaces a runtime.
- [ ] All phase and total measurements use monotonic timestamps from one controller. Browser and server events are correlated through controller-observed events rather than by mixing wall clocks. Retained attempt rows and any observed success or failure totals remain in attempt order only; statistics numerically sort and use only each successful attempt’s measured total and each timed-out attempt’s configured timeout bound. A non-timeout failed attempt may retain its observed total in attempt order but does not enter median, p95, or maximum; it is counted separately as a failure and target miss. A pre-start failure has no fabricated duration and is counted separately as a failure and target miss. Median is the conventional median of the numerically sorted durations, averaging the two middle values for an even count. p95 is the nearest-rank value at `ceil(0.95 * n)` over that same complete statistical set of successful measured totals plus timed-out configured bounds; `n` is the count of that set, and for `n = 5` and `n = 10`, p95 is the maximum duration. Timestamp unit and precision, duration rounding, and these median/p95 formulas are predeclared and have finite calculation tests.
- [ ] Every cold and warm attempt records host and cgroup specifications, current pre-attempt load and memory in declared units, code-server/Chromium/Node versions, run/attempt/project IDs, runtime PID/start/port token, stable URL, browser context and cache state, phase timings, success or failure, screenshot/trace/network evidence from activation through cleanup or reuse, and cleanup or reuse result. Capture bounds are declared; unavailable or failed capture is retained as an explicit evidence failure rather than omitted.
- [ ] Public artifacts, logs, screenshots, traces, network records, URLs, and process metadata expose no internal authority, internal port, canonical path, credential, or secret. Exact authority correlation is retained only in access-restricted, non-public storage, and public/restricted records remain unambiguously joined by safe identifiers.
- [ ] The three-project continuity section executes the exact merged BL-014 switching sequence three times without retry. Every run retains project-specific terminal, editor, and runtime identity evidence; Product Metric 2 is continuity passed runs divided by three; any state crossing or loss is retained as a failure; and run-attributed resources are cleaned after each run.
- [ ] The capacity section runs fresh integrated-product cohorts of 3, 5, and 10 using the same documented fixtures, workload, sampling schedule, method, units, readiness rules, and completeness semantics as BL-004. The three-member cohort is the NFR-003 MVP gate and passes only with three ready distinct runtimes, successful workloads, complete required samples, passing responsiveness checks, and complete cleanup; the five- and ten-member cohorts remain findings. Every partial failure remains attributable. Raw samples and a comparison table are retained, the prior BL-004 baseline is linked for delta comparison, and unlike methods are never silently compared.
- [ ] A retained machine-readable summary reports cold median, p95, maximum, failures (including pre-start failures), and target misses; warm median, p95, maximum, failures, identity-changes, and target misses; continuity successes from 0 through 3; capacity 3/5/10 readiness, workload, resource, and cleanup results; Product Metric 2 as continuity passed runs divided by three; Product Metric 3 as the declared warm-navigation statistics against 2,000 milliseconds; and Product Metric 4 as the declared cold-start statistics against 15,000 milliseconds. Every formula, duration source, and source attempt ID is machine-validated.
- [ ] Cold NFR-002/Metric 4 is `met` only if exactly five attempts exist, there are zero failures including zero pre-start failures, and p95 is no more than 15,000 milliseconds. Warm NFR-001/Metric 3 is `met` only if exactly ten attempts exist, there are zero failures, all runtime identities remain unchanged, and p95 is no more than 2,000 milliseconds. Because p95 is the maximum for these counts, every timed attempt must be within the applicable target. Continuity Metric 2 is `met` only if exactly 3/3 runs pass with no state crossing or loss. Capacity NFR-003 is `met` only if the three-project gate passes completely; the 5 and 10 cohorts remain findings. Any required metric not `met` defaults to `blocker`.
- [ ] `miss-accepted` cannot be chosen autonomously and is valid only with an explicit retained approval record created after measurement that names the approver, reason, risk, follow-up backlog ID, and the exact retained evidence hash. Without that record, the metric remains `blocker`. The overall release disposition is `blocker` if any required metric is `blocker`; otherwise it is `met`, with any valid `miss-accepted` metric counting as nonblocking. The exact failed target and disposition source are retained, and no threshold change is accepted.

**Edge Cases**
- [ ] A cold or warm failure, timeout, or artifact-capture failure retains all observed partial phases, any observed total, a declared finite failure classification, artifact status, and cleanup or reuse audit. A timeout contributes its configured timeout bound as its statistical total. A non-timeout failed attempt retains any observed total but excludes it from median, p95, and maximum and counts separately as a failure and target miss. A pre-start failure has no fabricated duration and counts separately as a failure and target miss. No failed or timed-out attempt is discarded or automatically retried.
- [ ] Missing fixtures, missing prior BL-004 baseline evidence, no usable raw samples, unavailable required browser artifacts, or an invalid or missing approval record produces a specific prerequisite, evidence, or disposition failure; no assigned value, empty summary, or success-shaped fallback is accepted.
- [ ] The evidence validator rejects assigned or synthetic timings, missing or duplicate attempts, altered order, retries, mixed clocks, post-run plan changes, prewarmed cold identity, changed warm identity, omitted failures, incomplete cleanup, formula or source-ID mismatches, threshold substitution, an invalid missing or autonomously created `miss-accepted` record, unsafe public evidence, and capacity comparisons made with unlike methods.
- [ ] Cleanup and reuse audits operate only on exact run-attributed resources and apply explicit boundary outcomes: after each cold attempt, its runtime and transient resources are absent; after each warm attempt, no duplicate or transient run-attributed resource remains while the expected healthy runtime identity and listener remain unchanged; after each continuity run, its owned resources are absent; after every section and the final run, all section-owned browser/process groups, proxy sockets, runtime PIDs/listeners, terminal commands, API/web services, databases, and fixtures are absent. Residual audits report measured zero where absence is required and exact expected identity counts where warm reuse is required, while pre-existing resources and an unrelated control listener survive unchanged until separate cleanup.
- [ ] Concurrent invocation cannot interleave attempts, combine run IDs or overwrite retained evidence. Controller or host interruption retains completed and partial records; a later invocation cannot reuse or merge them, and stale active ownership is rejected unless exact cleanup and an absence audit complete. Each rejection is bounded, nonzero, and leaves unrelated resources unchanged.

**Verification**
- [ ] Finite repository validation accepts one complete positive evidence set and rejects one controlled mutation for each named invalid class: missing/duplicate/order/retry, assigned or mixed-clock timing, plan or threshold substitution, cold/warm identity violation, omitted failure, timeout-bound, non-timeout failure inclusion, or pre-start duration violation, missing artifact, unsafe disclosure, median or nearest-rank p95 formula/source mismatch, invalid or autonomous approval, incorrect overall disposition, incomparable capacity method, and cleanup leakage. Formula cases include even-count median and nearest-rank p95 for n = 5 and n=10. Each rejection has an inspectable nonzero or invalid-evidence classification.
- [ ] One no-retry execution on the repository-documented designated host produces all 5 cold, 10 warm, 3 continuity, and 3/5/10 capacity records in the declared order, retains raw and summarized evidence, reports every target comparison and disposition, and completes the final residual audit.
- [ ] Independent recomputation from retained plan, attempt IDs, and raw samples exactly matches all phase totals, numerically sorted median/p95/maximum values, timeout-bound inclusion and non-timeout failure exclusion, failure and miss counts, continuity result, capacity table, Product Metric 2–4 values, per-metric dispositions, and overall release disposition.
- [ ] Documentation records the command, designated host and environment, fixtures/workload, exact start/end definitions and usable consequence, attempt order/count, clock/formulas/statistics, cache/prewarm rules, failure and interruption treatment, unchanged targets and dispositions, evidence capture/redaction/retention, cleanup, observed results, BL-004 delta, and any approval/follow-up links.
- [ ] The repository full-validation command and the root-command-interface regression gates for BL-004 and BL-010–014 pass. Missing or superseded required gates are reported as validation failures rather than silently skipped. The designated measurement remains serial and is not unintentionally duplicated inside ordinary unit or full-validation loops.

## Repository Findings
- **Product requirements:** `PRD.md:643-663` defines warm navigation within 2 seconds, cold usable startup within 15 seconds on the target development host, and at least three simultaneous workbenches, with 5 and 10 measured before lifecycle policy. `PRD.md:1508-1545` defines Metric 2 as correct terminal/editor association, Metric 3 from selecting a running project to usable workbench, and Metric 4 from opening a stopped project to usable workbench. Metric 4 also says the initial target is reassessed after empirical prototype measurements; Issue #35 explicitly freezes 15,000 ms and rejects threshold substitution.
- **Current designated-host snapshot:** On 2026-08-13 this checkout is branch `feat/35-measure-mvp-performance` at `/workspaces/ascend` on Ubuntu 24.04.4 LTS host `03f809395a5d`, user `vscode` uid 1000, Node 22.23.2, code-server 4.131.0, Playwright 1.62.1, and Chromium 151.0.7922.34. `/proc/self/cgroup` is `0::/`; cgroup v2 reports `cpu.max=max 100000`, CPUs `0-19`, unlimited memory/high/swap/PID limits, `memory.current=6841516032`, and `pids.current=255`. The point-in-time host readings were load 4.68/4.87/4.53, MemTotal 32,774,812 KiB, and MemAvailable 13,090,924 KiB.
- **BL-004 host and prerequisite contract:** `apps/api/src/workbench-capacity-prerequisites.ts` (`checkCapacityPrerequisites`) requires the exact OS patch, hostname, user/uid, Git common directory, code-server version, BL-001 fixture, `/proc` files, and root cgroup-v2 context before starts. `apps/api/src/workbench-capacity-contract.ts` fixes `CAPACITY_COHORTS` to 1/3/5/10, a 1,200,000 ms command bound, sequential 30,000 ms starts, five idle and five active positions at 0–4 seconds, a 1,000 ms `/usr/bin/true` probe, and a 7,000 ms workload with a 10,000 ms/4,096-byte bound.
- **BL-004 method and retained baseline:** `apps/api/src/workbench-capacity-coordinator.ts` starts raw BL-001 proof code-server members against the same canonical fixture, applies the same direct Node workload, samples after each target offset, cleans each cohort, and freezes only the three-member gate. `apps/api/src/workbench-capacity-sampling.ts` derives process-tree CPU from `/proc` tick deltas at `CLK_TCK=100` without 20-CPU normalization, sums `VmRSS` KiB, and records `/proc/loadavg`, MemAvailable, and MemTotal minus MemAvailable. The committed run under `project/work-items/11-bl-004-establish-the-workbench-capacity-baseline/implementation/evidence/853037e6-5dab-43cf-bcf8-61f1e8bbdb18/` completed all 1/3/5/10 cohorts, 19/19 workloads, complete samples, cleanup, and fixture integrity; its three-member gate passed.
- **BL-004 comparability facts:** The baseline one-minute load range was 2.39–3.28 and minimum MemAvailable was 18,467,380 KiB. The current single snapshot is 1.40 above that maximum load and 5,376,456 KiB below that minimum available memory. This is not itself an eligibility decision because neither the PRD nor BL-004 documents an allowable pre-run load or memory envelope. The BL-004 baseline is raw code-server over one shared fixture, whereas Issue #35 requires fresh integrated-product 3/5/10 cohorts and distinct ready runtimes; method and fixture equivalence therefore require explicit interpretation before a delta can be called comparable.
- **BL-004 validator boundary:** `validateCapacityEvidence` in `apps/api/src/workbench-capacity-contract.ts` rejects any cohort order other than 1/3/5/10 and requires five retained or explicitly absent positions for both windows. `just verify` invokes only `proof-workbench-capacity-audit`, not the expensive episode. Issue #35 orders only fresh 3/5/10 capacity cohorts after the other sections while also requiring BL-004 regression gates, so the old retained schema and the new issue plan are distinct evidence scopes.
- **BL-010 runtime proof:** `docs/project-runtime.md`, `ProjectRuntimeManager.start` in `apps/api/src/project-runtime-manager.ts`, and `test-results/bl-010/project-runtime/episode.json` show `/healthz/` readiness, health-checked reuse, immutable PID/start/port identity, memory-only ownership, and exact shutdown. The current generated episode records 572 ms against 15,000 ms, but this ends at runtime health and is not an integrated browser workbench-usable measurement.
- **BL-011 routing proof:** `docs/stable-workbench-routing.md`, `apps/api/src/workbench-proxy-manager.ts`, and `CORE-COMPONENT-260812-stable-workbench-proxy.md` establish the stable project prefix, five-second proxy HTTP/WebSocket bounds, safe controller-observed transport classifications, exact proxy-before-runtime shutdown, and restricted authority handling. The designated route proof uses three fresh no-retry workflows and six classified sockets, but it does not measure NFR-001 or NFR-002 from Project Home activation.
- **BL-012 integrated navigation timing:** `tests/e2e/home-workbench-timing.ts` uses `Date.now`, a 220,000 ms episode budget, and broad phase bounds. In the current generated real-process artifact `test-results/bl-012/browser-real-process.json`, Open-to-Explorer workbench readiness is 6,439 ms and the later terminal-operations phase is 4,804 ms. `tests/e2e/home-workbench-real-process.spec.ts` starts the first interval at `open.click` and ends after `.monaco-workbench` plus the Explorer sentinel; terminal creation and visible command consequences occur later. These values cannot be substituted for Issue #35 totals because they use a wall clock and do not share the required terminal-prompt usable endpoint.
- **BL-013 fixtures and isolation:** `tests/e2e/project-runtime-isolation.spec.ts`, `docs/project-runtime.md`, and `test-results/bl-013/runtime-isolation/three-project-chromium.json` use three disposable Git repositories with project-specific Explorer/editor/terminal/Git state, prove pairwise-distinct runtime identities, one explicit B replacement, peer continuity, and ten resource classes at zero after cleanup. Its declared scope is immediate isolation only, not BL-014 continuity or performance.
- **BL-014 sequence, clock, and continuity:** `BL014_FIXTURES`, `BL014_INITIAL_START_ORDER`, `BL014_OPEN_REENTRY_ORDER`, and `BL014_TRANSITION_ORDER` in `apps/api/src/session-switching-contract.ts` define A/B/C fixtures, initial B/C/A starts, the B/C/A/B/C Open re-entry order, and 24 transitions. `tests/e2e/session-switching.spec.ts` uses `process.hrtime.bigint`, a 240,000 ms episode bound, 30,000 ms browser-operation bounds, one 250 ms A counter, network/workflow joins, and twelve cleanup classes. The current public artifact completed in 93.645 seconds with one initial start per project and zero residuals; same-context continuity passed, while fresh-context server terminal and browser editor restoration were both recorded as `unsupported`.
- **BL-014 timing is not Metric 3 evidence:** Controller-observation deltas for the five B/C/A/B/C re-entries in the current artifact are 2,122.280, 2,086.875, 1,909.559, 2,278.960, and 2,205.970 ms. `beginTransition` observes the pre-action surface before keyboard activation and `finishTransition` observes after Explorer readiness and socket checks, but no activation timestamp, terminal-ready prompt endpoint, or retained total field exists. These derived deltas are therefore useful risk context only and are not comparable Issue #35 warm totals.
- **Activation and usable endpoint gaps:** `openWorkbench` in `apps/web/src/App.tsx` records UI state and queues `window.location.assign`, but exposes no performance timestamp. `browserShell` in `apps/api/src/workbench-navigation-shell.ts` has a 15,000 ms document acquisition timeout and `cache: no-store`. Existing browser readiness helpers require `.monaco-workbench` and Explorer content; none records the common Explorer-plus-ready-terminal-prompt consequence required by Issue #35.
- **Browser artifact behavior:** `playwright.config.ts` configures `trace: on-first-retry`; designated BL-010–014 recipes use one Chromium worker with `--retries=0`. Existing proofs retain selected network ledgers and failure artifacts, but the current global configuration does not produce a trace for every successful no-retry attempt. BL-014 explicitly inventories and clears cookies, local/session storage, CacheStorage, service workers, and browser cache only for its fresh-context case.
- **Commands and harness:** The root `justfile` is the canonical command interface and contains focused BL-004 and BL-010–014 gates plus authoritative `verify`; no BL-015 measurement recipe exists. `.harness/engineering-harness.md` delegates `harness checks` to `just verify`, gives it a 600,000 ms default budget, and documents boot as non-persistent/test-backed. The current `verify` runs designated BL-010–014 serial gates and audits; the BL-014 verification summary reports harness boot at 401,431 ms. The new designated measurement must remain separate from ordinary validation duplication per the issue criteria.
- **Statistics and disposition state:** Application, test, docs, and architecture searches found no current nearest-rank p95 or `miss-accepted` approval contract. `apps/api/src/workbench-presentation-contract.ts` has only a three-value middle median for BL-003. Product metric references outside Issue #35 remain confined to `PRD.md`.

## Constraints
- Preserve the issue-defined counts and order: 5 cold, 10 warm in `A/B/C/A/B/C/A/B/C/A`, 3 continuity runs using the merged BL-014 sequence, then fresh 3/5/10 capacity cohorts, without retries or omitted records.
- Use the unchanged 15,000 ms and 2,000 ms targets and the issue-defined complete-set median, nearest-rank p95, timeout-bound, failure, target-miss, approval, and overall-disposition rules. The PRD reassessment sentence does not authorize a threshold change in this issue.
- Preserve one controller monotonic clock for cold/warm phase and total observations. Existing `Date.now` BL-012 phase records and server-local elapsed values are not interchangeable with the required controller timeline.
- Keep BL-010 as sole runtime process owner and preserve health-checked reuse, immutable warm PID/start/port identity, no automatic restart, exact per-project ownership, and memory-only runtime state.
- Keep browser transport under the stable project prefix, retain full-page presentation and native Home/workbench navigation, and preserve proxy-before-runtime shutdown ordering and five-second transport bounds.
- Preserve public-evidence redaction: no canonical path, internal authority/port, credential, secret, protected payload, or unsafe URL/process metadata. Exact authority correlation is restricted and owner-readable; public joins use safe execution, attempt, project, workflow, and opaque project-token identifiers.
- Reuse the BL-004 sampling schedule, workload, units, readiness/completeness semantics, exact identity cleanup, and comparison caveat. Unlike raw and integrated methods cannot be silently compared.
- Operate only on exact run-attributed processes, groups, listeners, sockets, commands, database files, fixtures, and browser resources. Unrelated and pre-existing controls survive until separately cleaned.
- Use root `justfile` recipes as the project command surface; the governed harness wraps `just verify` and must not become a second command implementation. Research remains a leaf stage and performs no lifecycle orchestration.
- Keep generated browser/capacity evidence within repository-controlled retention boundaries and preserve existing tracked BL-004 evidence and regression behavior.

## Relevant ADRs and Core-Components
- `ADR-260808-typescript-monorepo`: fixes the single-host TypeScript stack and one direct host code-server runtime per active project; explicitly recognizes significant multi-process resource use.
- `ADR-260810-full-page-browser-workbench-presentation`: makes full-page code-server with the Ascend header authoritative for desktop Chromium; tablet remains non-authoritative.
- `ADR-260812-in-process-workbench-reverse-proxy` and `ADR-260812-browser-navigation-shell`: fix stable same-origin routing, in-process proxy ownership, native document navigation, shell acquisition, redaction, and shutdown ordering.
- `ADR-260808-governed-engineering-harness`: keeps `.harness` as the governed adapter while root `justfile` owns commands and boot remains test-backed.
- `CORE-COMPONENT-260808-runtime-lifecycle-error-handling`, `CORE-COMPONENT-260808-host-process-environment`, and `CORE-COMPONENT-260808-filesystem-path-safety`: constrain non-root direct launch, deterministic environment, exact canonical fixtures, health/reuse identity, no automatic retry, isolated per-project ownership, and exact cleanup.
- `CORE-COMPONENT-260812-stable-workbench-proxy` and `CORE-COMPONENT-260808-structured-runtime-logging`: constrain stable transport, safe controller correlation fields, public/restricted evidence separation, five-second proxy bounds, and protected-data exclusion.
- `CORE-COMPONENT-260810-sqlite-persistence-lifecycle`: requires explicit disposable database ownership and exact sidecar cleanup for integrated fixtures.
- `CORE-COMPONENT-260806-project-command-interface`, `CORE-COMPONENT-260806-agent-executable-acceptance-criteria`, `CORE-COMPONENT-260806-rpiv-stage-contract`, `CORE-COMPONENT-260808-engineering-harness-delivery-contract`, and `CORE-COMPONENT-260808-development-standards`: govern root commands, finite inspectable criteria, stable work-item paths, leaf-worker observations, validation ownership, and generated-artifact boundaries.
- `project/architecture/ADR/DECISION-LOG.md` records these as accepted/adopted decisions. Nothing in Issue #35 requests a change to process, proxy, presentation, navigation, persistence, or command ownership.

## Risks and Open Questions
- **Designated-load eligibility:** No repository document defines acceptable pre-attempt load, MemAvailable, `memory.current`, or PID-count ranges for normal target conditions. What fixed eligibility rule distinguishes an auditable designated-host run from a loaded but technically prerequisite-compliant run?
- **Capacity fixture equivalence:** BL-004 uses one canonical BL-001 fixture for raw independent code-server members; BL-013/014 use three distinct disposable Git fixtures through the integrated product. What exact fixture mapping satisfies both distinct integrated runtimes and the requirement to reuse BL-004 fixtures without creating an unlike-method comparison?
- **Capacity scope mismatch:** The historical validator requires 1/3/5/10, while Issue #35 requires fresh 3/5/10 after other sections. How is the prior one-member plumbing record represented in delta interpretation without changing the requested section order?
- **Usable consequence:** Existing proofs separately observe runtime health, stable document/Explorer readiness, and terminal consequences. The exact terminal prompt text and browser-observable readiness semantics shared by cold and warm attempts are not currently documented.
- **Cold sequence:** The issue requires an immutable exact five-project sequence but does not name it. The available A/B/C fixtures and historical B/C/A start order do not resolve that declaration.
- **Continuity sequence boundary:** BL-014 contains 24 transitions, 14 network workflows, initial B/C/A starts, B/C/A/B/C re-entries, history/reload/direct/fresh/close/reopen paths, and an unsupported fresh-context restoration outcome. The phrase exact merged BL-014 switching sequence needs an unambiguous retained boundary for each of the three runs.
- **Warm risk signal:** Four of five derived current BL-014 re-entry deltas exceed 2,000 ms, but their endpoints are not Issue #35 endpoints. The target outcome is unknown until comparable measurements exist.
- **Clock correlation:** Browser actions, Playwright observations, runtime events, shell document acquisition, and proxy events currently use a mixture of `process.hrtime.bigint`, `Date.now`, and component-local elapsed clocks. Only some records carry controller observation timestamps.
- **Artifact completeness and capture bounds:** Current successful no-retry Playwright runs do not automatically retain traces, and existing proof schemas differ in screenshots, traces, network rows, and failure artifacts. Repository-wide retention size and access-restricted storage limits are not documented.
- **Interruption and stale ownership:** BL-004 has an active guard and partial-evidence behavior; BL-010–014 browser proofs have separate cleanup owners. There is no current integrated run ownership or interruption record spanning all requested sections.
- **Approval authority:** No repository contract identifies authorized approvers, approval storage, or backlog-link validity for `miss-accepted`. Without explicit post-measurement approval, the issue requires blocker disposition.
- **Existing volatile artifacts:** `test-results/bl-010` through `bl-014` are generated/ignored and may be regenerated by ordinary validation. They are useful repository observations but are not substitutes for immutable Issue #35 evidence.
- **Overall command budget:** Existing root full validation and BL-014 proof are already long-running; no finite bound is declared for the combined 5/10/3/3-cohort measurement plan, and ordinary harness checks have a 600,000 ms default budget.
