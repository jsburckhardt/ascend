{"timestamp":"2026-07-20T06:30:00Z","verb":"lint","inference":"No lint command is declared in package.json (scripts only defines typecheck). The harness had to infer that lint coverage is absent and mark the verb unknown rather than wrap a real linter.","question":"What did the agent have to infer that the harness should have proved?"}
{"timestamp":"2026-07-20T06:30:00Z","verb":"test","inference":"No test command is declared in package.json and no test runner is configured. The harness had to infer that automated tests do not exist yet and mark the verb unknown.","question":"What did the agent have to infer that the harness should have proved?"}
{"timestamp":"2026-07-20T06:30:00Z","verb":"build","inference":"No build command is declared; the only tsc invocation is `typecheck` (tsc --noEmit), which is a type-check, not an emitting build. The harness had to infer there is no build step and mark the verb unknown.","question":"What did the agent have to infer that the harness should have proved?"}
{"timestamp":"2026-07-20T06:30:00Z","verb":"clean","inference":"No clean command is declared. The harness had to infer a safe clean scope (remove *.tsbuildinfo and dist/ only, preserving node_modules) rather than wrap a proven clean command.","question":"What did the agent have to infer that the harness should have proved?"}
