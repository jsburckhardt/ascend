# Test Plan: BL-018 Restart a Running or Failed Workbench

**Revision 5.** Sixteen validations, V-1 through V-16, unchanged in number. Every validation is finite, repeatable, offline, and executable from a root `justfile` recipe. Bounds are fixed before the first action of every scenario. Deterministic fakes prove race ordering, join behaviour, deadline abandonment, and stale settlement; they are never the evidence for the real release-and-replace episode, the real stale-connection behaviour, or the residual audit, which V-14 owns. `just verify` alone is never cited as evidence for a criterion other than AC-19, whose subject is the full gate.

**Revision 2 changes (historical record; its counts are superseded where the revision 4 and revision 5 paragraphs say so).** Independent validation returned NEEDS ATTENTION on revision 1. The affected validations are marked **[R2]**: V-1 asserts 17 failure categories, both corrected bounds, and the `RuntimeDeadlineScheduler` surface; V-4 and V-5 gain direct never-settling and cancellation-ignoring fallible-primitive tests, the phase-aware deadline settlements, and the collision-retry bound; V-7 asserts a 28-row exhaustive workbench table and drops revision 1's false negative assertion that restart release has no row; V-11 asserts the transport bound exceeds the manager bound; V-13 executes 51 scenarios and six mutation classes; V-15 replaces revision 1's non-existent "committed BL-011 matrix" with the real dynamic surfaces and asserts no committed evidence artifact changes.

**Revision 3 changes (historical record; its counts are superseded where the revision 4 and revision 5 paragraphs say so).** A narrowed independent recheck of revision 2 confirmed those six repairs held but found a CRITICAL late-ownership race and two related defects. The affected validations are marked **[R3]**: V-1 asserts the two new bound functions, both overall variants, and the two new four-member internal vocabularies; V-4 gains the never-resolving-admission retry row; V-5 gains the deadline-before-launch-resolution row and distinguishes `unaudited-retained` from `admission-unresolved`; V-6 gains bounded shutdown with an unresolved admission; V-7 gains the three late-callback rows; V-11 asserts 85,000 ms against the 81,000 ms ceiling; V-13 executes **60** scenarios and **seven** mutation classes; V-14 states and asserts the episode/residual-audit proof split; V-15 asserts the revision-3 documented bounds and model. The new **V-16** owns the pending replacement admission, the quarantine indexes, the restart-scoped callback wrappers, the detached continuation, and bounded shutdown end to end.

**Deadline scheduling in deterministic validations.** Every unit and matrix validation injects a recording `RuntimeDeadlineScheduler` whose `scheduleDeadline(delayMs, onDeadline)` records the pair and returns a recorded canceller. A test fires a deadline by invoking the recorded callback synchronously and asserts the canceller ran on the settlement path. No deterministic validation waits on a real 5,000 ms, 15,000 ms, 20,000 ms, 60,000 ms, 66,000 ms, or 81,000 ms allowance, and none uses fake timers or `processDependencies.sleep` to simulate one. Only V-14 uses real timers.

**[R3] Abandoned launches in deterministic validations.** Every scenario that needs an abandoned launch injects a launch promise the test itself holds, so the test decides whether it resolves, rejects, or never settles. No validation ever awaits such a promise, and none asserts a claim that would require awaiting it. The unresolved-admission cases assert bounded settlement and honest reporting, never absence.

**Revision 4 changes (historical record; the counts stated here are those of revision 4 and are superseded by the revision 5 paragraph below — the source-guard total is now 16, not 15).** Final narrowed validation of revision 3 rejected the plan for two collision-cleanup defects: confirming collision cleanups left stale ownership records, and non-confirming ones were discarded so a later restart could claim residual-free over an unaudited residual. No validation is added or removed — V-1 through V-16 stand. The affected ones are marked **[R4]**: V-1 asserts **18** failure categories, the **15** source-guard codes including the two new ones, and the five-value evidence unions; V-2 asserts the blocked-replacement settlement and the quarantine gate condition; V-3 asserts one ownership record after a collision-retrying success; V-5 asserts confirming-cleanup deletion, non-confirming quarantine-and-block with counted `ports.acquire` and `process.launch` calls, and the absence of any zero-residual claim; V-7 asserts a **29**-row exhaustive workbench table; V-11 asserts an **18**-member web mirror; V-13 executes **64** scenarios and **eight** mutation classes; V-15 asserts the revision-4 documented counts and rules; V-16 owns the identity-keyed cleanup wrapper, the `quarantined-residual` class, and quarantine reclamation end to end.

**[R4] Collision cleanup audits in deterministic validations.** Every scenario that exercises a collision retry fixes each attempt's cleanup audit triple before execution through the injected process adapter, so "confirming" and "non-confirming" are inputs, never observations of a real port. Blocking is asserted by counted primitive calls — `ports.acquire` and `process.launch` invocation counts after the abort — not by elapsed time, and no validation waits on a real allowance.

**Revision 5 changes.** Independent adjudication of revision 4 rejected the Plan for three findings; none adds, removes, or renumbers a validation, and V-1 through V-16 stand. The affected ones are marked **[R5]**: V-1 asserts **16** source-guard codes and the `residualCount: number | null` schema; V-5 gains the readiness-timeout non-confirming-cleanup variant and asserts the settlement reason selector directly; V-7 asserts a **17**-row `runtime:` subsequence and **six** new rows; V-11's expected evidence names the **18**-member web mirror; V-13 asserts the residual-claim predicate in both directions and the extended M-8 precedence rules; V-15 asserts **29** dynamic executions, the `'29-failure'` topic, and the `14 -> 18` literal correction; V-16 asserts `replacementBlockReason` assignment order and the four-branch blocking analysis.

**[R5] What the delivered launch primitive does and does not forward.** Every validation that exercises a non-confirming replacement cleanup asserts the corrected reading of `apps/api/src/project-runtime-process.ts`: a mid-loop collision rejects with the wrapper's `RuntimeFailure('restart-replacement-unconfirmed')` from the loop head at `:794`; a last-attempt collision rejects with `address-in-use-exhausted` at `:890`; a readiness, health, or early-exit failure rejects with its own already-typed category, because the rethrow at `:883-888` returns it before reading `input.signal.aborted`; and only a non-`RuntimeFailure` error reaches `abortFailure(input.signal)` at `:886`. Blocking is asserted on all four branches by counted `ports.acquire` and `process.launch` calls; the retained category is asserted to come from the manager's settlement reason selector, never from the launch error.

## Test V-1: Restart vocabulary, transition-target mapping, bounds, and source-contract guards

- **Type:** Unit and contract (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-1, T-8
- **Acceptance Criteria:** AC-1, AC-2, AC-3, AC-7, AC-8, AC-9, AC-12
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-contract.test.ts` and `apps/api/test/runtime-restart-evidence.test.ts` over `project-runtime-contract.ts`, `runtime-restart-evidence.ts`, and the real manager, process, restart-route, and stop-route sources read from disk. `apps/web/test/runtime-restart-client.test.ts` covers the web mirror.

### Steps
1. Assert `RUNTIME_ENTRY_STATES` has exactly 6 members in fixed order and `publicRuntimeState` maps all six plus `undefined`, with `restarting -> Starting`.
2. Assert `PUBLIC_RUNTIME_STATES` still has exactly 4 members and `RUNTIME_STATES` exactly 3, and that `restarting` is absent from both.
3. Assert `RUNTIME_LIFECYCLE_TARGETS` has exactly 6 members and `publicRuntimeStateForLifecycleTarget` maps all six, with `restarting -> Starting` and `stopped -> Stopped`.
4. Assert `RuntimeLifecycleEvent['event']` covers exactly 9 names, that `publicRuntimeStateForLifecycleEvent` agrees for all nine, and that a mismatched target throws.
5. **[R2][R4]** Assert `RUNTIME_FAILURE_CATEGORIES` has exactly **18** members in fixed order, each with a message, and that the four new messages (`restart-release-unconfirmed`, `restart-deadline-exceeded`, `runtime-restarting`, `restart-replacement-unconfirmed`) contain no path, PID, port, authority, command, environment value, or host detail.
6. Assert `RUNTIME_RESTART_OUTCOMES` has exactly 2 members, `RUNTIME_RESTART_REJECTION_CATEGORIES` exactly 7, `RUNTIME_STOP_REJECTION_CATEGORIES` exactly 7, `RUNTIME_STOP_OUTCOMES` still 3, and all four frozen.
7. **[R2]** Assert `createProjectRuntimeConfig` defaults `restartSettlementAllowanceMs` to 1,000, accepts an override, and rejects zero, negative, and non-integer values.
7a. **[R2]** Assert `runtimeReplacementBoundMs` returns **60,000** at defaults, equals `collisionAttempts * (readinessTimeoutMs + runtimeStopOverallBoundMs)` for three override sets, and rises proportionally when `collisionAttempts` rises — the correction for revision 1's false claim that the replacement phase is bounded by `readinessTimeoutMs` alone.
7b. **[R3]** Assert `runtimeRestartOverallBoundMs(config, false)` returns **66,000** at defaults and equals `runtimeStopOverallBoundMs + runtimeReplacementBoundMs + restartSettlementAllowanceMs`, and that `runtimeRestartOverallBoundMs(config, true)` returns **81,000** and equals `runtimeRestartReleaseBoundMs + runtimeReplacementBoundMs + restartSettlementAllowanceMs`, for the same three override sets. Assert every bound is a positive safe integer, **[R4]** that `requiresQuarantineResolution` is the only non-config parameter, that both functions are pure (identical inputs give identical outputs and read no clock, environment, or module state), and that the `true` variant is strictly greater than the `false` variant for every override set.
7d. **[R3]** Assert `restartQuarantineReleaseBoundMs` returns **15,000** at defaults and equals `collisionAttempts * runtimeStopOverallBoundMs`, and `runtimeRestartReleaseBoundMs` returns **20,000** and equals `runtimeStopOverallBoundMs + restartQuarantineReleaseBoundMs`, for the same override sets.
7e. **[R3][R4]** Assert `RESTART_ADMISSION_PHASES` is frozen with exactly **4** members in the order `launch-pending`, `materialized-quarantined`, `absent-confirmed`, `audited-absent`; that `RESTART_QUARANTINE_AUDIT_STATES` is frozen with exactly **4** members in the order `unaudited`, `reclaiming`, `audited-absent`, `audited-unconfirmed`; that neither shares a member with `PUBLIC_RUNTIME_STATES`, `RUNTIME_ENTRY_STATES`, `RUNTIME_LIFECYCLE_TARGETS`, or `RUNTIME_FAILURE_CATEGORIES`; **[R4]** that neither gained a member in revision 4, because `audited-unconfirmed` is exactly the state a non-confirming replacement cleanup produces; and that `RuntimeUnresolvedAdmission` has exactly the three members `projectToken`, `admissionId`, `phase` with no path, PID, port, or authority value.
7f. **[R4] Public vocabulary counts after revision 4.** Assert exactly: **18** failure categories, 2 restart outcomes, 7 restart rejection categories, 10 restart route error categories, 10 stop route error categories, 9 lifecycle events, 6 entry states, 6 lifecycle targets, 4 public states, 3 snapshot states, and **29** `WORKBENCH_FAILURE_TABLE` rows. Assert that revision 4 widened exactly three of them — failure categories, workbench rows, and the web mirror — and that `RUNTIME_RESTART_REJECTION_CATEGORIES` is still 7 with no member added for the blocked replacement. Assert no admission, quarantine, or attempt-audit value appears in any of them.
7c. **[R2]** Assert `RuntimeDeadlineScheduler` is satisfied by `defaultRuntimeDeadlineScheduler` and by `defaultRuntimeTerminationPrimitives`, that `RuntimeTerminationPrimitives` still exposes all seven members, that neither `now` nor `scheduleDeadline` returns a promise, and that a cancelled `scheduleDeadline` handle never invokes its callback.
8. **[R2][R4]** Assert the web `RUNTIME_FAILURE_CATEGORIES` and `RUNTIME_FAILURE_NOTICES` are identical to the API list at **18** members with bounded notice text for each.
9. **[R2]** Run `validateSelectedRestartSource` over the real manager, process, restart-route, stop-route, and workbench-proxy-contract sources and assert zero violations; then inject each guarded pattern — a `stop(`/`start(` manager call from the restart path, an ungated launch, a `registered` install in the restart path, a non-catalog event name, a `runtime.stop.succeeded` emission from the restart path, a fifth public-state literal, a bound read outside `ProjectRuntimeConfig`, a `processDependencies.sleep(` inside the restart body, a `scheduleDeadline(` count other than two, an uncancelled deadline arm, an untyped `phaseController.abort(` argument, a settlement branch inferring its phase from anything but `gateConfirmed`, a `launch(` call passing a derived `config`, a `WORKBENCH_FAILURE_TABLE` missing one proxied runtime category, **[R3]** a `launch(` not preceded by a `pendingAdmissions.set(` in the same block, an `onOwned:`/`onCleanup:` argument that is not the restart wrapper identifier, a `recordCleanup(`/`registerOwnership(` on the late or quarantine path, an `add(` of the launch-promise identifier to any task set, a `no-record` branch that does not read `pendingAdmissions`, a settlement path with no `void <launchPromise>.then(` continuation, **[R4]** an installed `restartOnCleanup` branch that deletes by project ID or by a key not derived from the cleanup audit's `pid`, `processStartTime`, and `port` or that calls `recordCleanup(` from either wrapper branch, and a non-confirming installed cleanup branch with no synchronous `phaseController.abort(new RuntimeFailure('restart-replacement-unconfirmed'))` or that does not assign that same instance to `replacementBlockReason` before the abort call, **[R5]** and a replacement-phase settlement branch that derives a retained failure category or a `replacementAuditState` from the caught launch error without first reading `phaseSignal.reason` as a typed `RuntimeFailure` — and assert the specific violation code for each. **[R5]** Violation codes added by this issue total **16**, and the sixteenth is `restart-settlement-reason-precedence-missing`.
10. Assert `RuntimeRestartInvariantError` carries no stack and no protected value.

### Expected Result
Every vocabulary is frozen at its fixed count, every mapping is exhaustive, every bound is a validated positive safe integer, and every guarded source pattern is rejected with its own violation code.

### Expected Evidence
**[R2][R4]** The vocabulary count table (**18** failure categories, **29** workbench rows), the two mapping tables, **[R3]** the bound table with `runtimeReplacementBoundMs` 60,000 ms, `restartQuarantineReleaseBoundMs` 15,000 ms, `runtimeRestartReleaseBoundMs` 20,000 ms, and `runtimeRestartOverallBoundMs` 66,000 ms / 81,000 ms plus their derivations and rejections, the two frozen four-member internal vocabularies, the unchanged-public-vocabulary table, the `RuntimeDeadlineScheduler` conformance result, the source-guard violation table including all **[R5] 16** codes, and a zero-match protected-value scan over every new message and notice string.

## Test V-2: Manager restart eligibility, claim, gate ordering, and settlements

- **Type:** Unit (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-2
- **Acceptance Criteria:** AC-1, AC-2, AC-3, AC-8, AC-9, AC-12
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-manager.test.ts` with the delivered injectable `launch`, `processDependencies`, `now`, `findProjectById`, and `recordEvent` boundaries, a controlled clock, and recorded terminate/audit primitives.

### Steps
1. Drive every row of the eligibility table: absent entry; unreleased `registered`; released `registered`; `starting`; `running`; `stopping`; `restarting`; `failed`; `shuttingDown`; unpersisted project. Assert the exact outcome, category, public state before and after, `entryMutations`, `identitiesCreated`, and `identitiesTerminated`.
2. Assert the claim is synchronous: instrument the entry map so any `await` between the eligibility read and the `restarting` install is detectable, and assert none occurs.
3. Assert the accepted claim emits exactly one `runtime.restart.requested` with `from` equal to `running` or `failed` and `to` equal to `restarting`.
4. **Gate ordering.** Assert the launch dependency is invoked zero times and no port is acquired until the release audit reports all three absences. Run three gate cases: a confirmed audit (launch invoked once, after the audit), an audit missing one absence (launch never invoked), and an audit whose component observations did not all complete (launch never invoked).
5. **[R3][R4]** Drive all **five** prior-resource classes for a `failed` entry — `live-record`, `absent-record`, `no-record`, `pending-admission`, and **[R4]** `quarantined-residual` — and assert the release mode, the signal-delivery record, and the gate outcome for each. The first three gate through; `pending-admission` and `quarantined-residual` gate only per the resolution table and are owned by V-16. Assert that `no-record` is recorded **only** when `pendingAdmissions.has(projectId)` is false, and that an entry whose project holds an unresolved admission is never classified `no-record`.
6. Drive every row of the settlement table and assert the installed entry, the ownership-record change, the per-phase cleanup counts, the emitted terminal event, the returned result, and the resulting public state.
7. Assert that after every settlement no entry remains in `restarting`.
8. Assert a settlement whose exact `restarting` object was replaced throws `RuntimeRestartInvariantError`, installs nothing, emits nothing, and records no additional cleanup.
9. **[R4]** Assert the success path removes the prior ownership record and registers the replacement's, that the project holds exactly **one** ownership record at settlement however many collision attempts preceded it, and and that the replacement snapshot carries the same `projectId`, `canonicalPath`, `stableRoute`, and `ownerToken` with a different `pid` and `processStartTime`.

10. **[R4] Blocked-replacement settlement.** With `gateConfirmed: true`, make one collision attempt's cleanup audit non-confirming. Assert the settlement row is `rejected` / `replacement-failed` with retained category `restart-replacement-unconfirmed`, that the retained `failed` entry references the quarantined identity, that the project's `priorResourceClass` on the next restart is `quarantined-residual`, that the next restart's gate stays closed until every quarantine record for that project reports `audited-absent`, and that neither settlement emits a zero-residual claim.

### Expected Result
Every eligibility row, every prior-resource class, every gate case, and every settlement row behaves exactly as the action plan fixes them, with no launch before a confirmed gate, no entry left in `restarting`, **[R4]** and exactly one ownership record per settled success.

### Expected Evidence
The eligibility result table, the gate-ordering counters (`spawnsBeforeGate: 0` in every row), the settlement table with entry, ownership, cleanup, event, result, and state columns, **[R4]** the per-settlement ownership-record count, the blocked-replacement row with its retained `restart-replacement-unconfirmed` category and quarantined identity, and the identity pair for every success.

## Test V-3: Joined concurrency, sequential replacement, and cardinality

- **Type:** Unit (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-2
- **Acceptance Criteria:** AC-10, AC-11
- **Priority:** High

### Setup
The V-2 harness with barrier-controlled terminate and launch fakes so callers can be released deterministically without sleeps.

### Steps
1. Issue eight concurrent `restart` calls for one eligible `running` project before the release phase settles. Assert exactly one accepted operation, one terminate invocation, one launch invocation, one `runtime.restart.requested`, one terminal event, eight identical resolved results with the same `projectId` and `outcome`, and settlement of all eight inside `runtimeRestartOverallBoundMs`.
2. Repeat step 1 for an eligible retained `failed` project and for a restart that settles as `release-unconfirmed`, asserting all eight callers receive the same non-success.
3. Issue three sequential successful restarts of one project. Assert three distinct replacement identity tuples, three `runtime.restart.requested` and three terminal events, and after each settlement exactly one entry, one ownership record, one running snapshot, and one listener.
3a. **[R4] Collision retries leave one record.** Drive a restart whose first two replacement attempts collide with a confirming cleanup audit each and whose third succeeds. Assert `ownership` holds exactly **one** record for that project at settlement, keyed to the third attempt's identity; that the two collided identities were each deleted by their own `pid:processStartTime:port` key and not by project ID; and that the two deletions removed nothing belonging to the surviving attempt. Then issue the next restart and assert its prior-resource inventory contains exactly **one** `live-record` handle and that the release phase performs exactly **one** termination — the direct proof that stale collision records cannot accumulate.
4. After the third settlement assert zero retained prior identities, zero attributable descendants, zero retained prior failures, zero surviving listeners for released generations, zero live timers, and zero in-flight operations through `audit()`.
5. Assert restarts for two different projects never share an entry, generation, operation promise, snapshot, failure, or cleanup record.

### Expected Result
Eight concurrent requests join one accepted restart with one release and one replacement; three sequential restarts produce three distinct replacements and leave no residual; **[R4]** a collision-retrying success leaves exactly one ownership record and the next restart releases exactly one handle.

### Expected Evidence
`joinedCallers: 8`, `acceptedRestarts: 1`, `releasePhaseTerminations: 1`, `replacementLaunches: 1`, the eight identical results, the three distinct identity tuples, the three event pairs, and the post-sequence `audit()` snapshot with zero counts.

## Test V-4: Release-phase failures and retained-failure retryability

- **Type:** Unit (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-2
- **Acceptance Criteria:** AC-2, AC-8
- **Priority:** High

### Setup
**[R2]** The V-2 harness with recorded termination primitives, a controlled clock, and the recording `RuntimeDeadlineScheduler`. Revision 1 injected `sleep` here; that is now prohibited, because the restart arms both deadlines with the trusted synchronous `scheduleDeadline` primitive and never touches a fallible promise for scheduling.

### Steps
1. Drive an escalation that cannot confirm release. Assert `rejected` / `release-unconfirmed`, `replacementLaunches: 0`, `identitiesCreated: 0`, a retained `failed` entry carrying `restart-release-unconfirmed`, a retained prior ownership record, and no `Stopped` observation on any surface.
2. Drive a termination fault. Assert the same settlement, that the fault is surfaced explicitly rather than swallowed, and that no entry remains in `restarting`.
3. Drive a termination that never returns. Assert it is abandoned at the release bound, that `lateTerminationSettlements` increments exactly once, that the late settlement mutates, signals, audits, and emits nothing, and that the settlement is `release-unconfirmed`.
3a. **[R2] Never-settling, cancellation-ignoring release primitive.** Inject a `terminate` that returns a promise which never settles and never registers an `abort` listener on its signal. Fire the recorded release backstop. Assert: the public `restart` promise settles `rejected` / `release-unconfirmed`; the entry is `failed` carrying `restart-release-unconfirmed`; `replacementLaunches: 0` and `identitiesCreated: 0`; the prior ownership record is retained; the release arm records `source: 'trusted-scheduler'` and `cancelled: true`; the phase abort reason is exactly `RuntimeFailure('restart-release-unconfirmed')`; and the restart body contains no `sleep` call. Then let the harness advance every remaining scheduled task and assert the still-pending termination has produced zero entry mutations, zero events, zero installs, and zero cleanup records. This is the direct proof that a fallible injected primitive cannot defeat the bound — the exact failure revision 1's `processDependencies.sleep` race admitted.
3b. **[R2] Cancellation-ignoring release primitive that settles late.** Inject a `terminate` that ignores its signal and resolves a fully confirmed audit triple after the backstop fired. Assert the settlement is `release-unconfirmed` and byte-identical to row 3a in result, entry, retained category, ownership record, public state series, and event table; that `lateTerminationSettlements` increments exactly once; and that the late confirmed audit is applied to nothing — it does not retroactively pass the gate, launch a replacement, or convert the settled failure into a success.
4. Assert the retained `Failed` project is immediately eligible for another restart, and that a second restart of the same project with a now-confirmable release succeeds and reports `Running` with no `failureCategory`.
5. Repeat the eligibility assertion after two consecutive unsuccessful restarts, then assert `manager-shutdown` is returned once `shuttingDown` is set.
6. Drive the `no-record` and `absent-record` prior-resource classes and assert both gate through as `already-absent` with `signalDelivery: not-attempted` and no termination call for the `no-record` case.
7. **[R3] Retry against a never-resolving pending admission (scenario 54).** Starting from a restart abandoned at its overall deadline with an injected launch that never settles and never reported ownership, immediately accept a second Restart. Assert: the second restart is **accepted** — Issue #41's retryability is preserved and acceptance is never gated; its release backstop is armed at `runtimeRestartReleaseBoundMs` = **20,000 ms** rather than 5,000 ms because **[R4]** `requiresQuarantineResolution` was true at acceptance; the admission never resolves inside that bound; the second restart settles `rejected` / `release-unconfirmed` with `replacementLaunches: 0` and `identitiesCreated: 0`; the newly retained `failed` entry carries `restart-release-unconfirmed` **and** re-references the same `pendingAdmissionId`; the admission is still `launch-pending` with `admissionsCreated: 0` for the second restart; and no absence, zero residual, or completed audit is claimed anywhere.

### Expected Result
Every release failure returns an explicit bounded non-success, launches nothing, never reports `Stopped` or success, and leaves the project retryably `Failed` with a bounded actionable category.

### Expected Evidence
**[R3][R4]** Matrix rows 2, 3, 4, 20, 21, 45, 46, 49, and **54** with the release mode, the **five**-value prior-resource class, the retained category, the retained ownership record, `replacementLaunches: 0`, the deadline arm records with the 5,000 ms and 20,000 ms variants, the late-settlement count, the `admission` row object with `phaseAtSettlement: 'launch-pending'` and `resolution: 'unresolved'`, and the successful retry's identity and `Running` report.

## Test V-5: Replacement-phase failures, settlement reason precedence, and residual claims **[R5]**

- **Type:** Unit (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-2
- **Acceptance Criteria:** AC-9
- **Priority:** High

### Setup
**[R2]** The V-2 harness with the recording `RuntimeDeadlineScheduler` and an injected `launch` that fails at spawn, fails at readiness, resolves after the restart deadline, never settles at all, and — in the collision variants — reproduces the delivered `launchReadyRuntime` retry shape by rejecting with `address-in-use` for a controlled number of attempts.

### Steps
1. Drive a spawn-class replacement failure after a confirmed release. Assert `rejected` / `replacement-failed`, a retained `failed` entry carrying the underlying bounded launch category, and the removed prior ownership record.
2. Drive a readiness-timeout replacement failure. Assert the same result shape with `readiness-timeout` retained.
3. For both, assert the replacement's exact identity is terminated and audited, its ownership record is removed when that audit confirms all three absences, and the recorded `residualCount` is the integer `0` — an audited-absent claim covering process, owned descendant, listener, timer, and in-flight startup — while `teardownResidualCount` is separately the integer `0` for the harness's own fixture cleanup. **[R5]** Assert the two fields are distinct properties and that neither is derived from the other.
4. Drive a launch that resolves ready *after* the restart deadline. Assert it is terminated and audited, never installed, counted in `lateReplacementSettlements`, and that the settled entry is unchanged by its arrival.
5. Assert no replacement failure changes the project registration, its four persisted fields, or its fixture manifest.
6. **[R2] Never-settling, cancellation-ignoring launch primitive that already reported ownership.** After a confirmed release, inject a `launch` that fires `onOwned` and then never settles and never observes its signal. Fire the recorded overall deadline. Assert: the public `restart` promise settles `rejected` / `replacement-failed` with retained failure category `restart-deadline-exceeded`; the abort reason is exactly `RuntimeFailure('restart-deadline-exceeded')` and **never** an untyped reason; `replacementAuditState` is `'unaudited-retained'`; the replacement's `onOwned` record is **retained**; the row records `residualCount: null` — **[R5]** the schema's only representation of a withheld residual claim — makes **no** zero-residual claim for the replacement, and records no replacement cleanup audit; and both deadline arms record `cancelled: true`. Then run manager shutdown and assert exactly one sweep terminates and audits that retained replacement identity.
6a. **[R3] Deadline fires before `process.launch()` resolves (scenario 52).** After a confirmed release, inject a `launch` that has acquired its port and is awaiting `process.launch()` — it has fired **no** `onOwned` and holds a promise the test controls. Fire the recorded overall deadline. Assert: the public `restart` promise settles `rejected` / `replacement-failed` with retained category `restart-deadline-exceeded` inside the declared bound; `replacementAuditState` is **`'admission-unresolved'`**, not `'unaudited-retained'` and not `'audited-absent'`; `replacementOwnershipRecords: 0`; the pending admission exists, was created **before** the launch call, and is still `launch-pending`; the retained `failed` entry carries its `pendingAdmissionId`; **no** replacement cleanup audit exists; and the row records `residualCount: null` and asserts **no** zero-residual claim for that replacement, **[R5]** which mutation class M-7 independently rejects in both directions — a `0` where the predicate requires `null`, and a `null` where the predicate requires `0`. This is the direct proof that ownership *ignorance* is no longer recorded as ownership *absence*.
7. **[R2] Post-gate deadline classification.** With `gateConfirmed: true`, fire the overall deadline and assert the manager result category is `replacement-failed` (so `RUNTIME_RESTART_REJECTION_CATEGORIES` stays at 7), the route maps it to `runtime_replacement_failed` at **500**, the terminal event classification is `restart-deadline-exceeded`, the public projection reports `Failed` with `restart-deadline-exceeded`, the Home notice is the bounded `restart-deadline-exceeded` sentence, and **no** result, event, projection, notice, log record, or evidence row anywhere reports `manager-shutdown` while `shuttingDown` is false. Assert the project is immediately eligible for another explicit Restart.
8. **[R2] Pre-gate deadline classification.** With `gateConfirmed: false`, fire the overall deadline and assert the settlement is identical to an ordinary unconfirmed release in result, retained category, ownership record, launch count, identity count, public state series, and event table — proving the deadline never launches anything before the gate and never reports a replacement failure it did not attempt.
9. **[R2][R4] Collision-retry bound and record hygiene.** Drive a replacement whose first two attempts reject with `address-in-use` — each with a **confirming** cleanup audit fixed by the fixture — and whose third succeeds. Assert three launch attempts, two intervening cleanup terminations, one installed replacement, and a settlement inside `runtimeReplacementBoundMs`. Assert additionally that each collided attempt's ownership record was deleted by its own `pid:processStartTime:port` key at the moment its confirming audit arrived, that `ownership` never held more than one record for the project after any attempt returned, that exactly one record survives settlement, and that the restart body wrote exactly **one** project-keyed `recordCleanup` for the replacement phase while the three per-attempt audits were retained in `replacementAttemptAudits`. Then drive a variant whose every configured attempt collides and assert the retained category is `address-in-use-exhausted`, proving that reachability is preserved because restart passes the manager's own `config` with its configured `collisionAttempts` rather than a derived single-attempt config.

9b. **[R4] Non-confirming collision cleanup blocks the attempt loop.** Drive a replacement whose first attempt rejects with `address-in-use` and whose fixed cleanup audit reports `listenerAbsent: false` (the collided port is still held). Assert, as counted primitive calls and not as timing: `process.launch` was invoked exactly **once** and `ports.acquire` exactly **once** after the abort, so no successor was started; the abort reason is exactly `RuntimeFailure('restart-replacement-unconfirmed')` and never an untyped reason or `manager-shutdown`; that identity moved into `quarantinedOwnership` with `auditState: 'audited-unconfirmed'` and its exact audit stored under `quarantineCleanups`; `ownership` holds **zero** records for the project; `replacementAuditState` is `'quarantined-unconfirmed'`; **[R5]** `residualCount` is `null` and the row makes **no** zero-residual claim and **no** success claim; the public settlement is `rejected` / `replacement-failed` with retained category `restart-replacement-unconfirmed`; and the whole settlement completes inside the declared replacement allowance. Repeat once with `processAbsent: false` and once with `processGroupAbsent: false` to prove any single non-confirming member blocks.
9c. **[R4] Quarantined residual is reclaimable and gates the next restart.** After 9b, accept a second Restart. Assert its prior-resource class is `quarantined-residual`, that it acquires no port and launches nothing until the quarantined identity is terminated and re-audited, that a confirming reattempt deletes exactly that quarantine record and lets the gate pass inside `restartQuarantineReleaseBoundMs` within the 20,000 ms release variant, and that a still non-confirming reattempt settles `rejected` / `release-unconfirmed` with `replacementLaunches: 0` and keeps the record for the shutdown sweep. Assert no path between 9b and a confirming reattempt ever reports zero residual for that project.

9d. **[R5] Non-collision replacement failure with a non-confirming cleanup settles from the phase-abort reason, not the launch error.** Drive a replacement that reaches `process.launch()` and then fails **readiness** — no `address-in-use` anywhere — with the fixture's cleanup audit triple fixed non-confirming (`listenerAbsent: false`; repeat once with `processAbsent: false` and once with `processGroupAbsent: false`). Assert, as counted primitive calls and not as timing: `ports.acquire` was invoked exactly **once** in total and **zero** times after the abort; `process.launch` exactly **once** in total and **zero** times after the abort; the wrapper assigned `replacementBlockReason` exactly once, with the same `RuntimeFailure('restart-replacement-unconfirmed')` instance it then passed to `phaseController.abort(...)`, and strictly before that call; the launch promise itself rejected with `readiness-timeout`, proving the delivered rethrow at `project-runtime-process.ts:883-888` returned the already-typed failure without consulting `input.signal.aborted`; and yet the settlement's `settlementReasonSource` is `'phase-abort'`, its `launchRejectionCategory` is `'readiness-timeout'`, its public settlement is `rejected` / `replacement-failed`, and the **retained** failure category is `restart-replacement-unconfirmed`. Assert the identity moved into `quarantinedOwnership` with `auditState: 'audited-unconfirmed'` and its exact audit under `quarantineCleanups`; `ownership` holds **zero** records for the project; `replacementAuditState` is `'quarantined-unconfirmed'`; `residualCount` is `null`; and the row makes **no** zero-residual claim, **no** `audited-absent` claim, and **no** success claim anywhere. Repeat the same assertions for a **last-attempt collision** whose cleanup is non-confirming, where the launch promise rejects with `address-in-use-exhausted` from `project-runtime-process.ts:890` rather than with the wrapper's reason. Then drive the negative control: the same readiness failure with a **confirming** cleanup, and assert `settlementReasonSource` is `'launch-error'`, the retained category is `readiness-timeout`, `replacementAuditState` is `'audited-absent'`, and `residualCount` is the integer `0` — proving the selector narrows only the branches it must. Finally drive the precedence control: fire the overall deadline **before** the wrapper aborts, and assert the retained category is `restart-deadline-exceeded` because `AbortController` keeps the first reason, while `replacementAuditState` is still `'quarantined-unconfirmed'` and `residualCount` is still `null`.

### Expected Result
A confirmed release followed by a failed replacement returns an explicit bounded non-success, reports `Failed` with the underlying actionable category, and leaves nothing behind; **[R4]** an unconfirmed collision cleanup is quarantined, blocks the attempt loop, and can never be reported as a zero residual; **[R5]** and an unconfirmed cleanup on any other branch — readiness, health, early exit, or a last-attempt collision — reaches exactly the same retained category and audit state, because the settlement is classified from the restart's own typed phase-abort reason rather than from whatever the launch primitive happened to reject with.

### Expected Evidence
**[R3][R4]** Matrix rows 22, 23, 24, 47, 48, 50, **52**, and **[R4] 61, 62, 63, and 64** with the replacement cleanup audit triple and `residualCount: 0` on every `audited-absent` row, `replacementAuditState: 'unaudited-retained'` with a retained ownership record and **[R5]** `residualCount: null` on the ownership-reported abandoned-launch row, `replacementAuditState: 'admission-unresolved'` with `replacementOwnershipRecords: 0` and **[R5]** `residualCount: null` on row 52, `timersSurvivingReturn: 0`, both deadline arm records with `fired` and `abortReasonCategory`, the retained launch or `restart-deadline-exceeded` category, **[R4]** the `replacementAttempts` row object carrying every attempt's key, audit triple, and deletion outcome, the post-settlement ownership-record count of one on rows 61 and 64 and zero on rows 62 and 63, the `ports.acquire` and `process.launch` call counts proving the block, `replacementAuditState: 'quarantined-unconfirmed'` with `residualCount: null` on rows 62 and 63, **[R5]** row 62's two executed branches — branch A the mid-loop collision and branch B the readiness timeout — each carrying its own `launchRejectionCategory` (`address-in-use` and `readiness-timeout`) with the same `settlementReasonSource: 'phase-abort'` and the same retained `restart-replacement-unconfirmed`, **[R5]** the negative control's `settlementReasonSource: 'launch-error'` with retained `readiness-timeout` and `residualCount: 0`, **[R5]** `teardownResidualCount: 0` on every listed row, and the unchanged registration and fixture digests.

## Test V-6: Conflict matrix, reuse recheck, and shutdown coordination

- **Type:** Unit (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-3, T-14
- **Acceptance Criteria:** AC-3, AC-12, AC-14
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-conflicts.test.ts` with the V-2 harness plus a fake proxy consumer driving `start` and `ownsSnapshot`.

### Steps
1. Assert `start()` on a `restarting` entry throws `runtime-restarting` without mutating, terminating, auditing, or emitting.
2. Assert `stop()` on a `restarting` entry returns `rejected` / `restart-in-progress` with zero mutations and zero events.
3. Assert `restart()` on a `starting` entry returns `start-in-progress` and the in-flight start still reaches `Running`; on a `stopping` entry returns `stop-in-progress` and the in-flight stop still settles normally.
4. Assert a healthy reuse verdict delayed past the claim yields `runtime-restarting`, never the doomed snapshot, with zero mutations and zero events; repeat for an unhealthy verdict yielding the winner-consistent failure.
5. Assert a proxy-path `start` during `restarting` receives the bounded failure and forwards nothing, and that `ownsSnapshot` is false for the replaced snapshot in both winner orders.
6. Assert `reportPublicStates` returns `Starting` with no `failureCategory` for a `restarting` entry, and that `inspectEntries` reports the new state with a `waiterCount` of zero.
7. Assert manager shutdown rejects new restarts with `manager-shutdown`, awaits every in-flight restart before the ownership sweep, records no concurrent cleanup for one exact identity, and produces an honest shutdown result.
8. **[R3] Bounded shutdown with an unresolved admission (scenario 55).** Leave a project with an admission still `launch-pending` from an injected launch that never settles, then call `shutdown()`. Assert: the promise **resolves** rather than hanging; the launch promise is absent from `completionTasks`, `backgroundTasks`, `restartTasks`, and `stopTasks`, so the delivered unbounded drain never observes it; `awaitedAbandonedLaunch: false`; awaiting `restartTasks` was finite because each restart operation carries its own deadline; the result carries exactly one `unresolvedAdmissions` entry of `{ projectToken, admissionId, phase: 'launch-pending' }` with opaque values only; `status` is **`'failed'`**, never `'ok'`, and `RuntimeShutdownResult` has exactly **3** members; and no zero-residual claim, completed audit, or clean-shutdown statement is emitted for that admission in the result, in an event, in evidence, or in documentation.
9. **[R3] Quarantine sweep.** With one `audited-unconfirmed` and one `unaudited` quarantine record present, run `shutdown()` and assert the sweep claims only the `audited-unconfirmed` record through the compare-and-set to `reclaiming`, terminates and audits it exactly once, deletes it only when the audit reports all three absences, leaves the `unaudited` record to its in-flight continuation and reports it, and never double-terminates an identity a continuation is already terminating.

### Expected Result
Every conflict resolves to exactly one documented bounded classification, no loser mutates or emits, and shutdown never races an in-flight restart.

### Expected Evidence
**[R3]** Matrix rows 28-32, 39, 41, 43, and **55** with the conflict outcome, the loser's zero-mutation and zero-event counts, the shutdown cleanup phase table, and the `shutdown` row object recording `status`, `unresolvedAdmissionCount`, `quarantineSwept`, `awaitedAbandonedLaunch: false`, and `elapsedClass`.

## Test V-7: Stale prior-generation settlements and connection severance

- **Type:** Unit (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-4, T-14
- **Acceptance Criteria:** AC-4, AC-14
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-stale.test.ts` with barrier-controlled primitives so each stale settlement can be released after the successor is installed, plus an in-process proxy harness with a real loopback fake upstream for the connection cases.

### Steps
1. Release a prior termination after the release deadline and assert `appliedToSuccessor: false`, `successorMutations: 0`, `successorEvents: 0`, and an unchanged successor identity.
2. Release a prior process-exit settlement after the successor is installed and assert the guarded transition loses silently with no cleanup and no event.
3. Release a prior health verdict after the claim and assert `runtime-restarting` or the winner's retained failure, with the successor unchanged.
4. Release a launch settlement after the restart deadline and assert it is terminated, audited, and never installed. **[R2]** Repeat with a launch that never settles at all and assert `replacementAuditState: 'unaudited-retained'`, a retained ownership record, no absence claim, and the successor-free settled entry unchanged.
4a. **[R3] Late `onOwned` is quarantined, never registered (scenario 58).** After settlement, fire the wrapped `onOwned` with an exact identity. Assert `ownership.size` is unchanged, `quarantinedOwnership` grows by exactly one keyed by `admissionId + '|' + ownershipKey(record)` with `auditState: 'unaudited'`, `entryMutations: 0`, `eventsEmitted: 0`, and `currentCleanupMutations: 0`; then run one exact audit reporting all three absences and assert the quarantine record is deleted exactly once, and that an audit missing any one absence leaves it in place as `audited-unconfirmed`.
4b. **[R3] Late `onCleanup` cannot overwrite current evidence (scenario 57).** Install a successor restart for the same project, then fire the abandoned restart's wrapped `onCleanup`. Assert `lastCleanup(projectId)` still returns the **successor's** audit byte-for-byte, `cleanupOutcomes` is unmutated, the late audit is stored only under its exact quarantine key, and no entry, successor snapshot, route, or lifecycle event changed. This is the direct proof that the project-keyed `recordCleanup` can no longer be reached from a settled restart.
4c. **[R3] Late launch resolution after shutdown (scenario 56).** Call `shutdown()` while a launch is abandoned, then resolve that launch. Assert the detached continuation quarantines the exact identity, terminates it once with no caller signal, records the audit under the quarantine key, increments `lateReplacementSettlements`, and mutates no entry, no ownership record, no `cleanupOutcomes`, no successor, and no event — the manager is already gone.
5. Release a prior-generation proxy acquisition after the claim and assert `ownsSnapshot` is false and nothing is forwarded.
5a. **[R2]** Drive a stable-route request against a project held in `restarting` and assert it resolves to the bounded `503` `workbench_restarting` failure, that `workbenchFailure('runtime:runtime-restarting')` returns a row rather than throwing `Unknown workbench failure category`, and that `WORKBENCH_FAILURE_TABLE` has exactly **[R4] 29** rows in the declared order, including the published `runtime:restart-replacement-unconfirmed` row.
5b. **[R2]** Drive a proxy-path `start()` whose reuse recheck loses to an installed retained failure carrying each of `stop-unconfirmed`, `restart-release-unconfirmed`, and `restart-deadline-exceeded`, and one whose recheck loses to an in-flight stop carrying `runtime-stopping`. Assert each resolves to its published bounded `503` row with the exact code and safe message and that none throws `Unknown workbench failure category`. Revision 1 asserted negatively that no row exists for `runtime:restart-release-unconfirmed`; that assertion was **false** and is deleted — `reuseOwnershipFailure` returns `installed.failure` for a retained failed winner, so any retained category is proxy-reachable through `ProjectRuntimeManager.start()`.
5c. **[R2]** Assert exhaustiveness mechanically: `workbenchFailure` resolves for **every** member of `WorkbenchFailureCategory` with zero throws; the table's `runtime:`-prefixed category subsequence equals `RUNTIME_FAILURE_CATEGORIES` minus `manager-shutdown`, in contract order; the `runtime:` group has exactly **[R5] 17** rows; and the compile-time `Exclude<WorkbenchFailureCategory, TableCategory>` / `Exclude<TableCategory, WorkbenchFailureCategory>` checks both resolve to `never`, so a future runtime category cannot silently reintroduce the throw.
5d. **[R2][R5]** Assert every one of the **six** new rows carries status `503`, a distinct code, a safe message containing no path, port, PID, authority, or command, and is classified retryable by the delivered navigation shell.
6. Establish a prior-generation HTTP request and a prior-generation WebSocket, perform a successful restart, and assert both can no longer communicate with the released runtime while the successor is unaffected.
7. Perform a fresh navigation through the unchanged stable route and assert it acquires the successor snapshot; assert `sessionContinuityClaimed: false` is recorded and that no assertion, notice, or document claims editor or terminal continuity.

### Expected Result
No stale prior-generation settlement of any class can terminate, replace, route traffic to, emit for, or change the state of the successful replacement, and prior-generation connections are severed while the stable route still reaches the replacement.

### Expected Evidence
**[R3]** Matrix rows 10, 11, 37, 38, 39, 40, 41, 51, **56**, **57**, and **58** with the stale settlement class, the three zero counters, the `lateCallbacks` row object with `ownershipMapMutations: 0`, `entryMutations: 0`, `currentCleanupMutations: 0`, and `eventsEmitted: 0`, the connection record, the fresh-navigation acquisition result, the **[R4] 29**-row workbench failure table with its recomputed `WORKBENCH_FAILURE_TABLE_SHA256`, the exhaustiveness equality result, and one bounded `503` response record per newly published category.

## Test V-8: Public projection across generations

- **Type:** Unit (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-2
- **Acceptance Criteria:** AC-3
- **Priority:** High

### Setup
The V-2 harness plus an in-process Fastify instance serving `GET /api/projects/runtime` and the delivered web reconciliation helper, so runtime, API, and Home-facing observations are taken from the same settled state.

### Steps
1. Observe all three surfaces at four points for a successful restart: at acceptance, after the release phase, before readiness, and after settlement. Assert `Starting`, `Starting`, `Starting`, `Running`.
2. Repeat for a `release-unconfirmed` restart, asserting `Starting`, `Starting`, `Failed` with `restart-release-unconfirmed`.
3. Repeat for a `replacement-failed` restart, asserting `Starting`, `Starting`, `Starting`, `Failed` with the underlying launch category.
4. Assert no observation in any series is `Stopped`, and that the manager never installs a `registered` entry during a restart.
5. Assert `Running` never appears while the replacement process exists but has not passed the `/healthz/` readiness contract, by holding the health fake at a failing verdict and observing all three surfaces.
6. Assert every observation on every surface is a member of the four public values.

### Expected Result
The selected project is `Starting` from acceptance until readiness or a terminal failure, never `Stopped` between generations, and `Running` only after the delivered readiness contract.

### Expected Evidence
Matrix rows 7, 8, 9 with the complete ordered `stateSeries` for all three surfaces and the four-value exhaustiveness assertion.

## Test V-9: Restart event semantics and cardinality

- **Type:** Unit (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-1, T-2, T-5
- **Acceptance Criteria:** AC-7
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-events.test.ts` with a recording `recordEvent` boundary and an in-process Fastify instance with a recording logger.

### Steps
1. Assert an accepted restart emits exactly one `runtime.restart.requested` and exactly one terminal event, for all three settlements.
2. Assert `foreignEventCount: 0` — no `runtime.stop.requested`, `runtime.stop.succeeded`, `runtime.start.requested`, `runtime.start.succeeded`, `runtime.start.failed`, or `runtime.health.changed` is emitted by the restart path in any settlement.
3. Assert every pre-acceptance rejection emits zero restart lifecycle events, for all seven rejection categories.
4. Assert a joined caller emits nothing and a lost claim emits nothing.
5. Assert every emitted name is a member of the NFR-015 catalog and that `serializeRuntimeEvent` carries only the opaque project token, the transition targets, the elapsed value, and the bounded classification.
6. Assert the route's `project.runtime.restart.rejected` and `project.runtime.restart.failed` operational records are emitted through the request logger, carry only the bounded route category, and are excluded from the lifecycle event set by name prefix.
7. Assert the outcome-agreement table: for each scenario the emitted terminal event, the manager result, the authoritative state report, and the Home notice all describe the same outcome.
8. Run the protected-value scan over every event payload, every route response body, every notice string, and every retained evidence row.

### Expected Result
Exactly one requested and one terminal restart event per accepted restart, zero events for every non-accepted path, zero foreign events, and full cross-surface outcome agreement with zero protected-value matches.

### Expected Evidence
Matrix rows 17, 18, 19 with the ordered event tables, `requestedEventCount`, `terminalEventCount`, `preAcceptEventCount: 0`, `loserEventCount: 0`, `foreignEventCount: 0`, the outcome-agreement table, and the zero-match disclosure scan.

## Test V-10: Restart route contract and the extended stop route

- **Type:** Integration (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-5
- **Acceptance Criteria:** AC-7, AC-12
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-route.test.ts` with an in-process Fastify instance, a stubbed `projectRuntime` returning each manager outcome, and an isolated project database.

### Steps
1. Assert the success response is exactly `{ id, outcome: 'restarted' }` at 200 with no extra key.
2. Assert each of the seven manager rejection categories maps to its documented route category and status, and that the body is exactly `{ error: { category } }`.
3. Assert an infrastructure fault and an identity mismatch both produce `runtime_restart_failed` at 500 without leaking the underlying error.
4. Assert an empty `:id`, a missing `:id`, and both collapsed paths produce `invalid_project_id` at 400.
5. Assert a query parameter, any body field, an oversized body, invalid JSON, an empty JSON body, and an unsupported media type each produce `invalid_restart_request` at 400.
6. Assert a percent-encoded `:id` round-trips to the exact stable project ID.
7. Assert the manager `restart` is invoked exactly once per accepted request and zero times for every 400 path.
8. Assert the stop route now has exactly 10 categories, that `runtime_restart_in_progress` maps `restart-in-progress` to 409, and that the nine delivered rows are unchanged.
9. Scan every response body for a path, PID, port, authority, command, environment value, credential, or raw error and assert zero matches.

### Expected Result
Both routes expose exactly their fixed vocabularies with their fixed statuses, reject every malformed request before touching the manager, and disclose nothing beyond the bounded category.

### Expected Evidence
The ten-row restart status/category table, the ten-row stop table with the delivered nine unchanged, the manager invocation counts, and the zero-match disclosure scan.

## Test V-11: Web restart transport, parser, and client-owned notices

- **Type:** Unit
- **Task:** T-6
- **Acceptance Criteria:** AC-6, AC-7, AC-12, AC-15
- **Priority:** Medium

### Setup
`apps/web/test/runtime-restart-client.test.ts` with a stub fetcher and fake timers, plus the delivered `apps/web/test/runtime-stop-client.test.ts` extended for the widened stop vocabulary.

### Steps
1. Assert `runtimeRestartEndpoint` percent-encodes the ID and produces the exact path.
2. Assert `parseRuntimeRestartResponse` accepts the exact success envelope and rejects extra keys, missing keys, a wrong ID, a non-object body, and an unknown outcome.
3. Assert it accepts each of the ten error categories only at its exact status and rejects a category at a wrong status, an unknown category, extra keys, and a non-object error.
4. Assert `sendRuntimeRestartRequest` returns `failure` / `invalid_project_id` for an empty ID without issuing a request.
5. Assert it returns `unknown` for a network rejection, a non-JSON body, a parse failure, an owner abort, and a timeout at `RUNTIME_RESTART_TIMEOUT_MS`.
6. Assert the timeout timer is cleared on every path.
7. Assert `RUNTIME_RESTART_NOTICES` has exactly one bounded sentence per category with zero protected-value matches.
8. Assert the web stop mirror: `RUNTIME_STOP_ERROR_CATEGORIES` has exactly **10** members including `runtime_restart_in_progress`; a **409** `runtime_restart_in_progress` body parses to a bounded failure rather than `unknown`; the same category at any other status is rejected; and `RUNTIME_STOP_NOTICES` carries one bounded sentence for it with zero protected-value matches.
9. **[R3]** Assert `RUNTIME_RESTART_TIMEOUT_MS` is **85,000** and strictly greater than **both** variants of `runtimeRestartOverallBoundMs` at defaults — the 66,000 ms ordinary bound and the **81,000 ms** caller-visible ceiling — so a restart that consumes its full server-side allowance, including one that had to resolve a pending admission, still returns a bounded category rather than degrading into `unknown`. Revision 1's 10,000 ms would have made `unknown` the routine outcome of any restart that hit a real allowance, and revision 2's 70,000 ms would have done the same for every pending-admission restart, defeating AC-12's bounded-vocabulary requirement in the browser. Assert the rule from `ADR-260815-per-project-lifecycle-activation` is satisfied against the ceiling and not merely against the ordinary bound.
10. **[R2][R4]** Assert the web failure-notice mirror: `RUNTIME_FAILURE_CATEGORIES` and `RUNTIME_FAILURE_NOTICES` in `apps/web/src/runtime-state.ts` have exactly **18** members identical to the API list, and that a public projection carrying `restart-release-unconfirmed`, `restart-deadline-exceeded`, `runtime-restarting`, or **[R4]** `restart-replacement-unconfirmed` renders bounded notice text rather than a fallback or an empty string.

### Expected Result
The restart transport classifies exactly the ten documented failures, never invents a success, and returns `unknown` for every unclassifiable outcome; and the stop transport classifies a restart-window 409 as a bounded failure instead of degrading it to `unknown`.

### Expected Evidence
**[R2]** The parser acceptance and rejection table, the transport outcome table, the cleared-timer assertion, the notice scan, the 10-member web stop vocabulary assertion with its 409 parse record, **[R3]** the `85,000 > 81,000 > 66,000` bound comparison, and **[R4]** the **18**-member web failure-notice mirror result.

## Test V-12: Project Home restart interaction, eligibility, accessibility, and per-project availability

- **Type:** Component (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-7
- **Acceptance Criteria:** AC-3, AC-6, AC-15
- **Priority:** High

### Setup
`apps/web/test/use-project-home-restart.test.tsx` and `apps/web/src/runtime-restart-component-matrix.test.tsx` with the delivered testing-library harness, a stub restart transport, a stub runtime-state loader, and three registered projects in known public states.

### Steps
1. Assert Restart is rendered for a project reported `Running` and for one reported `Failed`, and is absent for `Stopped`, `Starting`, and every card in a revision whose projection is unavailable.
2. Assert the control is a `<button>`, reachable by keyboard, activatable by Enter and Space, and carries its `aria-label`, `aria-describedby`, and `data-restart-project-id`.
3. Activate Restart for project A and assert exactly one transport request, `aria-busy` on A's card, A's Restart control disabled, and a polite pending announcement.
4. While A's restart is pending, assert B's Restart, Open, Stop, and Close controls are enabled and that activating B's Restart issues a second independent transport request that is neither refused nor queued.
5. Activate A's Restart three more times while pending and assert no additional transport request is issued.
6. Settle A successfully: assert the pending record is removed, exactly one additional runtime-state request is issued, the success announcement is made, and focus is on A's Restart control.
7. Settle a failure: assert a `role="alert"` notice with the bounded category text, a Retry restart action, the failure announcement, focus on that card's Restart control, and zero additional state requests.
8. Settle an unknown outcome: assert the explicit unknown block, zero automatic transport requests, zero automatic state requests, that activating Refresh runtime state issues exactly one read-only state request, and that no state is assumed and no second replacement is created.
9. Assert a settlement whose owner was superseded, and one whose project has left the list, are both discarded without mutating state.
10. Assert a pending restart does not change the delivered Open, Stop, or Close disabled expressions, and that the delivered stop and close component assertions still pass.

### Expected Result
Restart is offered only where eligible, activation is keyboard accessible and single-flight per project, all four outcomes reach assistive technology, focus returns to the activating control, other projects stay fully available, and the unknown outcome stays explicit and user-driven.

### Expected Evidence
Matrix rows 13, 14, 15, 16, 42 with the eligibility table by state, the activation and refusal counts, the ARIA and announcement records, the focus-return record, the peer-control availability table, and the state-request counts per outcome.

## Test V-13: Deterministic 64-scenario matrix, mutation rejection, integrity, and retained evidence **[R4]**

- **Type:** Acceptance (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-8, T-9, T-14
- **Acceptance Criteria:** AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-9, AC-10, AC-11, AC-12, AC-13, AC-14, AC-15, AC-16, AC-17, AC-20
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-matrix.test.ts` with the injectable dependency harness, an in-process Fastify instance, an isolated project database, two disposable project fixtures, one declared unrelated control process, one declared unrelated control listener, the delivered fixture-manifest schema, **[R2]** the recording `RuntimeDeadlineScheduler`, **[R3]** test-held launch promises for the abandoned-launch scenarios, **[R4]** per-attempt cleanup audit triples fixed by the fixture for the collision scenarios, and the fixed **64**-scenario catalog.

### Steps
1. Assert the catalog, schema version, declared bounds, and validator are fixed before execution and that scenario order and IDs match `BL018_SCENARIOS` exactly.
2. Capture before manifests for every declared fixture node and digests of the four persisted registration fields for both projects, plus peer and control identities.
3. **[R3][R4]** Execute all **64** scenarios in catalog order, recording for each the pre-declared four-class inventory, the ordered state series per surface, the eligibility and prior-resource classes, the gate record with `gateConfirmed`, both deadline arm records (`source`, `declaredMs`, `cancelled`, `fired`, `abortReasonCategory`), `replacementAuditState`, the identity pair, the release audit triple, the attribution record and ceiling, per-phase terminate and cleanup counts, `lateTerminationSettlements` and `lateReplacementSettlements`, ordered events with all five cardinality counters, the stale-settlement records, the connection record where applicable, peer and control digests, registration digests, fixture manifest digests, the elapsed class with `withinDeclaredBound`, **[R5]** `residualCount` (`number | null`, the operation's settlement residual knowledge) and `teardownResidualCount` (the integer `0`, the harness's validation-owned fixture cleanup) as two distinct fields, and **[R3]** the `admission`, `quarantine`, `lateCallbacks`, `taskSets`, and `shutdown` row objects with an `audit()` snapshot per row, **[R4]** plus the `replacementAttempts` row object recording each attempt's identity key, cleanup audit triple, deletion outcome, and the resulting `ports.acquire` and `process.launch` call counts.
4. Assert after every scenario that the selected project remains registered exactly once with unchanged ID, name, canonical path, and created-at, and that every fixture node's membership, content digest, permission mode, and recorded timestamp is unchanged.
5. Assert for rows 33-36 that the peer project remains `Running` with the same runtime identity, readiness verdict, route, connections, and registered metadata, and that the declared control process and listener retain their identities and availability.
6. Write measured monotonic timing to `test-results/bl-018/runtime-restart-timing.json` and assert each measured elapsed is within its configured bound.
7. Validate the matrix and assert acceptance.
8. **[R3][R4]** Apply one controlled corruption per validator rule across mutation classes **M-1 through M-8** — including a launch recorded before the gate, a non-zero `spawnsBeforeGate`, an incomplete release audit on a `restarted` row, a `Stopped` observation in a state series, a `Running` before readiness, a fifth state value, two accepted restarts on a joined row, two release terminations, two replacement launches, a wrong requested or terminal event count, a non-zero pre-accept, loser, or foreign event count, a stale settlement recorded as applied, a changed peer, control, registration, or fixture digest, **[R5]** a positive, negative, non-integer, `NaN`, non-numeric, or absent `residualCount`, a `teardownResidualCount` other than the integer `0`, a `teardownResidualCount` that counts a quarantined identity or an unresolved admission, an empty inventory, a `priorGenerationUsable: true`, a `sessionContinuityClaimed: true`, a protected-value match, a raw PID or port in an identity field, a wall-clock timestamp, a duplicate or reordered scenario, a duplicate execution or event ID, **[R2]** a deadline arm whose `source` is not `trusted-scheduler`, an uncancelled arm on a settled row, a `fired: overall` row whose `abortReasonCategory` is `manager-shutdown`, a `fired: overall`/`gateConfirmed: false` row carrying a launch, a `fired: overall`/`gateConfirmed: true` row retaining `restart-release-unconfirmed`, **[R5]** an `unaudited-retained` row whose `residualCount` is not `null`, **[R5]** an `audited-absent` row whose `residualCount` is `null` rather than the integer `0`, an `audited-absent` row whose replacement audit triple is incomplete, **[R3][R5]** an `admission-unresolved` row whose `residualCount` is not `null`, a `no-record` prior-resource class on a project holding an unresolved admission, a gate passed with `admission.resolution: 'unresolved'` or `resolutionOrder: 'after-gate'`, a launched row with `admission.createdBeforeLaunch: false`, a `lateCallbacks` object with a non-zero `ownershipMapMutations`, `entryMutations`, `currentCleanupMutations`, or `eventsEmitted`, a quarantine deletion without a confirming triple, a `quarantine.concurrentAttempts` above zero or `reattempts` above `reattemptClaims`, a `taskSets` flag recording an abandoned launch in a drained set, a `shutdown` object with `status: 'ok'` and a non-zero `unresolvedAdmissionCount`, a `shutdown` object with `awaitedAbandonedLaunch: true`, **[R4]** a `priorResourceClass` or `replacementAuditState` outside its **five** values, an `admission.phaseAtSettlement` outside `RESTART_ADMISSION_PHASES`, a `quarantine.auditStates` member outside `RESTART_QUARANTINE_AUDIT_STATES`, an `admission.deletions` above one, an `admission.admissionsCreated` above one for one accepted restart or non-zero for a joined caller, and — **[R4]**, as mutation class **M-8** — a `replacementAttempts` entry whose confirming audit left its ownership record undeleted, a row whose post-settlement ownership-record count is not one on `restarted` or not zero on every non-success, a deletion keyed to anything but the attempt's own `pid:processStartTime:port`, a non-confirming attempt audit with no matching `audited-unconfirmed` quarantine record, a non-confirming attempt followed by a further `process.launch` or `ports.acquire` call, **[R5]** a `quarantined-unconfirmed` row whose `residualCount` is not `null`, **[R5]** a row whose `settlementReasonSource` is `launch-error` while its own replacement phase created a quarantine record, a `phase-abort` row whose retained category differs from its recorded phase-abort reason category, a non-confirming cleanup row with an absent `launchRejectionCategory`, a `settlementReasonSource` outside its three values, **[R5]** a `quarantined-unconfirmed` row recording `outcome: 'restarted'` or a retained category that is neither `restart-replacement-unconfirmed` nor `restart-deadline-exceeded`, more than one project-keyed replacement-phase `recordCleanup`, and a `quarantineCleanups` key collision between two distinct identities — and assert each is rejected with its specific violation code.
9. Write the deterministic matrix to `test-results/bl-018/runtime-restart-matrix.json`, assert SHA-256 equality with the committed copy, re-read and revalidate the committed copy, and assert byte-identical reserialization.
10. Assert the prerequisite table: repository-local fixtures only, no network access, no credential, no hosted service, no unsupported hardware, no destructive environment action, no indefinite wait, and no manual judgment.

### Expected Result
**[R3][R4]** All **64** scenarios execute deterministically and offline within their declared bounds — every deadline fired through the recording scheduler, every abandoned launch held by the test, and every collision cleanup audit fixed by the fixture, so no scenario waits on a real allowance, on a promise it cannot control, or on a real port probe — every integrity and isolation assertion holds, the validator rejects every controlled corruption, and the disposable and committed artifacts are byte-identical and revalidate after re-read.

### Expected Evidence
The committed `runtime-restart-matrix.json` with its SHA-256, the disposable timing artifact, the mutation-rejection table with one violation code per rule, the registration and fixture digest tables, the peer and control identity tables, and the reserialization equality result.

## Test V-14: Designated real-host restart episode and independent residual audit

- **Type:** Designated proof (gated by `BL018_DESIGNATED=1`)
- **Task:** T-10
- **Acceptance Criteria:** AC-1, AC-4, AC-11, AC-17, AC-20
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-designated.test.ts` on the designated devcontainer with the configured local code-server, one disposable project fixture, loopback networking, and `--workers=1 --retries=0`. `apps/api/src/cli/runtime-restart-residual-audit.ts` runs afterwards as a separate process.

### Steps
1. Start one real code-server generation for the fixture through the manager. Record the exact root identity with its process start time, every owned process-group member identity with their start times, the owned process-group ID, and the loopback listener port.
2. Establish one real HTTP request and one real WebSocket to that generation through the stable workbench route and hold them open.
3. Call `restart` once. Record the release audit, the gate result, the replacement identity, and the replacement readiness verdict.
4. After settlement, assert out of band that the recorded prior root identity is absent, every recorded owned-group member identity is absent, the owned process group is absent, and the prior loopback listener refuses connections — and that all four observations were taken before the replacement's own identity was recorded as ready.
5. Assert the retained prior-generation HTTP request and WebSocket can no longer communicate, and that a fresh navigation through the unchanged stable route reaches the ready replacement.
6. Assert the replacement identity differs from the prior identity in pid and process start time for the same project, canonical path, stable route, and owner token.
7. Execute two further real restarts for the three-sequential requirement, asserting after each settlement exactly one live runtime identity and exactly one listener for that project.
8. Assert the registration's four fields and the fixture manifest are unchanged, and record the verbatim attribution ceiling and the explicit no-session-continuity statement.
8a. **[R3] The episode may not finish while it still knows about an unaudited resource.** Before writing its artifact and returning, assert every identity the episode recorded — the prior root identity, every recorded owned-group member, the replacement, and any quarantined late identity — is audited absent, and assert `audit().pendingAdmissions === 0` and `audit().quarantinedOwnershipRecords === 0` after every settlement and at teardown. The episode fails rather than finishing with an unresolved admission, so it never produces the never-settling-launch case; that case is proven only in V-13 and V-16, where the launch primitive is injected.
8b. **[R3] Record the proof split verbatim in both artifacts.** The episode is in-process and self-reported: it can hold real connections and read the manager's own indexes, but it cannot independently prove that a late-materialising process or listener closed, because the only observer is the process being asked to prove the claim. The residual audit is the authority for late process, owned-group, and listener closure because it re-probes recorded exact identities from a separate process; its ceiling is that it can only probe identities that were **recorded**, so an admission that never materialised has no tuple to probe and is reported as `unresolvedAdmissions`, never as a proven absence.
9. Write `test-results/bl-018/designated-episode.json` with `schemaVersion: 1`.
10. **[R4]** Assert the residual-audit CLI reports a non-empty `quarantinedIdentities` array with a non-zero exit code when given an episode artifact carrying an identity whose audit state is not `audited-absent`, and that no artifact, message, or exit code converts it into a zero residual. **[R5]** Assert the CLI reads the episode's `residualCount` as `number | null`, treats a `null` as an unresolved residual that forces a non-zero exit rather than as a satisfied check, and never coerces `null` to `0`; assert it reads the separate `teardownResidualCount` only as validation-owned fixture teardown and never as a manager absence claim. Run `just proof-runtime-restart-residual-audit`. Assert it re-probes, out of process, the recorded prior root identity, every recorded member identity, the owned process group, the prior listener, and the retained stale connection, writes `test-results/bl-018/residual-audit.json` with zero residuals for all seven classes **[R3]** plus an `unresolvedAdmissions` array, exits non-zero on any residual **or** any unresolved admission, and never converts an unresolved admission into a proven absence or a zero residual.

### Expected Result
One real host episode proves confirmed prior absence before replacement, severed prior-generation connections, a distinct ready replacement on the unchanged stable route, three sequential real replacements with one runtime and one listener after each, and an independently audited zero residual.

### Expected Evidence
`test-results/bl-018/designated-episode.json` with the identity closure, the connection outcomes, the ceiling text, **[R3]** the verbatim proof-split statement, and the final `pendingAdmissions: 0` / `quarantinedOwnershipRecords: 0` assertions; `test-results/bl-018/residual-audit.json` with zero residual counts and an empty `unresolvedAdmissions` array; **[R5]** the CLI's recorded treatment of a `null` `residualCount` as unresolved and of `teardownResidualCount` as validation-owned only; both disposable and re-provable by re-running their recipes.

## Test V-15: Documentation, commands, targeted regressions, and the full gate

- **Type:** Acceptance and regression
- **Task:** T-11, T-12, T-13
- **Acceptance Criteria:** AC-18, AC-19, AC-20
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-documentation.test.ts` plus the delivered documentation tests, the root `justfile`, and a clean working tree on the issue branch.

### Steps
1. **[R2]** Assert every documentation category in the Documentation Scope table contains its required content: eligibility, the two-phase sequence and gate, **[R3]** the declared bounds including the 5,000 ms ordinary release bound and the **20,000 ms** pending-admission release bound, the **15,000 ms** quarantine reclamation allowance, the **60,000 ms** replacement allowance with its `collisionAttempts x (readinessTimeoutMs + runtimeStopOverallBoundMs)` derivation, the 1,000 ms settlement allowance, the **66,000 ms** overall default and the **81,000 ms** caller-visible ceiling, and the **85,000 ms** browser transport bound with the statement that it must exceed that ceiling; the pending replacement admission with its four phases and creation-before-launch rule, the exact-identity quarantine indexes with their four audit states, the constrained post-settlement mutation rule, and honest bounded shutdown with `unresolvedAdmissions`; state and connection behaviour; the bounded outcomes and their counts; accessibility; privacy limits; evidence artifacts; cleanup; and the three command names.
2. **[R2][R4]** Assert the corrected counts appear at their exact stated locations: **18** failure categories (`README.md:73`, `docs/project-runtime.md:74`, `apps/api/src/routes/README.md:39`), **29** workbench catalog rows (`README.md:161`, `apps/api/README.md:123`, `docs/stable-workbench-routing.md` including the `29-failure` anchor phrase), 6 entry states, 6 transition targets, 9 emitted lifecycle event names (`apps/api/README.md:142` nine-name), 10 restart route categories, and 10 stop route categories (`apps/web/README.md:34` ten). Assert negatively that the three counts this issue does **not** widen are unchanged: `PRESENTATION_ASSERTION_IDS` at 23, `WORKBENCH_BROWSER_CLASSIFIER_VECTORS` at 23, and the `fourteen` workflow count in `docs/session-switching.md`.
2a. **[R3]** Assert negatively that no shipped document, notice, event, evidence artifact, or result claims a zero residual, a completed audit, or a clean shutdown for an unresolved pending admission, and that every documented bound names its revision-3 value.

2b. **[R5] Documented settlement precedence and residual encoding.** Assert `docs/project-runtime.md` states, in prose a reader can act on: that a replacement phase aborted by its own restart wrapper is classified from that typed abort reason and never from the launch error, so a readiness, health, early-exit, or last-attempt-collision failure whose cleanup did not confirm absence is reported as `restart-replacement-unconfirmed` with a quarantined identity; and that a withheld residual claim is published as an explicit `null` while a proven absence is the integer `0`, with validation-owned fixture teardown recorded in its own separate field. Assert negatively that no shipped document describes a withheld residual claim as a zero, as an absent field, or as a sentinel number, and that no shipped document states that every replacement failure carries the wrapper's reason out of the launch primitive.
3. Assert every shipped document explicitly preserves the BL-019 API-restart reconciliation boundary and the BL-020 running-or-failed project-close boundary, and assert negatively that no shipped document still states Restart is deferred.
4. Assert the explicit no-session-continuity statement appears in `docs/session-switching.md`, `docs/stable-workbench-routing.md`, and `apps/web/README.md`.
5. Assert the `justfile` defines `verify-runtime-restart`, `proof-runtime-restart`, and `proof-runtime-restart-residual-audit`, that `verify` references each exactly once in the fixed position after `proof-runtime-stop-residual-audit`, and that no delivered recipe body changed.
6. Run `just verify-runtime-restart`, `just proof-runtime-restart`, and `just proof-runtime-restart-residual-audit` independently and record success.
7. Run each named regression recipe independently and record success: `verify-project-runtime`, `verify-workbench-route`, `verify-home-workbench false`, `verify-project-runtime-isolation`, `verify-session-switching`, `verify-runtime-state`, `verify-runtime-stop`, `proof-runtime-stop`, `proof-runtime-stop-residual-audit`, `verify-mvp-performance`.
8. Run `just verify` and record its successful completion.
9. Assert the committed BL-018 matrix is byte-identical after regeneration and that the working tree is clean.
10. **[R2] BL-011 regression — corrected.** Revision 1 instructed asserting a regenerated committed BL-011 matrix with 24 executions. **No committed BL-011 evidence artifact exists.** `find project/work-items -name '*.json'` returns committed evidence only for BL-003, BL-004, BL-010, BL-015, BL-016, and BL-017, and BL-011's matrix is built at run time from `WORKBENCH_FAILURE_TABLE` inside `apps/api/test/workbench-route-acceptance.test.ts` and `apps/api/test/workbench-route-evidence.test.ts`. Assert instead, all executable as written:
    - a. `just verify-workbench-route` passes, and its dynamically constructed matrix reports **[R5]** `executions.length === WORKBENCH_FAILURE_TABLE.length === 29`, with `declaredCategories` equal to the table's category order and one real route execution per row (the **[R5] six** new `case-runtime-*` executions are derived automatically by the delivered loop, not hand-written).
    - b. `validateWorkbenchFailureMatrix` accepts the constructed matrix, and its `tableHash` equals the recomputed `WORKBENCH_FAILURE_TABLE_SHA256` — a value computed in memory, not stored in any tracked file.
    - c. `apps/api/test/workbench-proxy-contract.test.ts` passes with its row-count literals updated at lines 48 and 51 (23 -> **[R5] 29**), and `apps/api/test/workbench-route-documentation.test.ts` passes with its topic literal updated at line 64 (`'23-failure'` -> **[R5]** `'29-failure'`) against the **[R5] 29** documented rows in `docs/stable-workbench-routing.md`.
    - d. Record the SHA-256 of every committed BL-003, BL-004, BL-010, BL-015, BL-016, and BL-017 evidence artifact before and after the implementation and assert equality. **No committed evidence artifact is modified by BL-018**; exactly one is added, `runtime-restart-matrix.json`.
11. **[R2]** Assert the delivered BL-017 source guards still report zero violations after the widened vocabularies: `validateRouteSource` at its new expected count of 10 stop route categories, `validateStateLiterals` over the whole widened `project-runtime-manager.ts` and stop route, `validateManagerSource` for its four whole-file occurrence counts, and `NFR015_EVENT_CATALOG` accepting all three restart event names without a catalog edit. Assert the committed BL-017 matrix is byte-unchanged.

### Expected Result
Every affected document is accurate and reconciled, the three new recipes exist and are wired exactly once, every BL-010 through BL-017 regression gate passes independently, and the canonical full gate completes successfully.

### Expected Evidence
**[R2]** The per-file documentation assertion table with its exact stated locations, the `verify` recipe diff, the recorded successful output of each new and each regression recipe, the recorded successful `just verify` output, the committed BL-018 matrix SHA-256 unchanged after regeneration, the `just verify-workbench-route` run report showing **[R4] 29** dynamic executions and its recomputed table hash, the before/after SHA-256 equality table for every previously committed BL-003, BL-004, BL-010, BL-015, BL-016, and BL-017 evidence artifact, and the BL-017 source-guard zero-violation report.

## Test V-16: Pending replacement admission, quarantine, restart-scoped callbacks, and bounded shutdown **[R3]**

- **Type:** Unit and acceptance (gated by `BL018_ACCEPTANCE=1`)
- **Task:** T-14, T-1, T-2, T-3
- **Acceptance Criteria:** AC-1, AC-2, AC-8, AC-9, AC-12, AC-14, AC-17
- **Priority:** High

### Setup
`apps/api/test/runtime-restart-admission.test.ts` with the V-2 harness, the recording `RuntimeDeadlineScheduler`, and a launch primitive whose returned promise is held by the test so it can resolve, reject, or never settle on command. The harness records every `pendingAdmissions`, `quarantinedOwnership`, `quarantineCleanups`, `ownership`, `entries`, `cleanupOutcomes`, and event mutation with an ordering index, so "before the gate" and "after settlement" are asserted as orderings and not as timings. No test in this validation ever awaits an abandoned launch promise.

### Steps
1. **Admission precedes launch.** For every scenario that launches, assert the harness observed a `pendingAdmissions.set` for that project strictly before the first `launch` invocation, in the same synchronous step, with `admission.createdBeforeLaunch: true`. Assert `admissionId` is opaque — it matches `deriveProjectOwnerToken(projectId) + ':a' + <integer>` and contains no path segment, PID, or port — and that `admittedAt` came from `deadlineScheduler.now()` and not `Date.now()`.
2. **Admission cardinality.** Assert at most one admission per project at any instant; that eight joined callers produce `admissionsCreated: 1` and that joined callers create, resolve, and delete nothing; that a successful restart deletes its admission in the same synchronous settlement step that installs the `running` entry, leaving `pendingAdmissions` at zero for that project; and that `createPendingAdmission` throws `RuntimeRestartInvariantError` if one already exists.
3. **[R4] Five-value prior-resource classification.** Assert a project holding an unresolved admission is classified `pending-admission` on every path, that a project holding an uncleared quarantine record but no unresolved admission is classified **`quarantined-residual`**, that `no-record` is recorded only when both indexes are empty for that project, and that no code path collapses `pending-admission` or `quarantined-residual` to `no-record` or to `already-absent`. Assert the classifier's precedence is fixed and total: `live-record`, then `pending-admission`, then `quarantined-residual`, then `absent-record`, then `no-record`.
4. **Gate resolution table.** Drive all four rows and assert the recorded `admissionResolution`, `resolutionOrder`, `spawnsBeforeGate: 0`, and deletion count for each:
   - `absent-confirmed` — the gate may pass; `releaseMode: 'already-absent'`, `signalDelivery: 'not-attempted'`, `createdProcessCount: 0`, admission deleted exactly once.
   - `audited-absent` — the gate may pass; the recorded exact quarantine audits are reused, each quarantine record and then the admission deleted exactly once.
   - `materialized-quarantined` — **[R4]** reached either from a late continuation or from an installed non-confirming replacement cleanup, and identical in either case — the gate passes **only** after reclamation: every `audited-unconfirmed` record is claimed by the compare-and-set to `reclaiming`, terminated, and audited, and the gate passes only when every quarantined identity reports process, owned-process-group, **and** listener absence. Assert an audit missing any one of the three leaves the gate closed and settles `release-unconfirmed`.
   - not resolved inside the release bound — the gate never passes; settle `rejected` / `release-unconfirmed`, retain `restart-release-unconfirmed`, `replacementLaunches: 0`, `identitiesCreated: 0`, keep the admission, and re-reference it from the newly retained `failed` entry.
5. **Scenario 53 — the race itself.** Fire the overall deadline while `process.launch()` is still pending and no `onOwned` has fired, so the first restart settles `replacement-failed` / `restart-deadline-exceeded` with `replacementAuditState: 'admission-unresolved'`. Immediately accept a second Restart. Then resolve the first launch late. Assert, as strict orderings: the second restart is accepted; it installs **no** successor and performs **zero** launches and **zero** port acquisitions until the late predecessor identity has been terminated and its full process, owned-group, and listener absence triple confirmed; `resolutionOrder: 'before-gate'`; `spawnsBeforeGate: 0`; exactly one quarantine termination and exactly one deletion for that identity; and at every recorded instant across the whole interleaving there is at most **one** live generation for that project, canonical path, and stable route.
6. **Restart-scoped callback wrappers.** Assert the `onOwned` and `onCleanup` arguments at the restart's `launch` call site are the `restartOnOwned`/`restartOnCleanup` wrappers and never `start`'s callbacks. **[R4]** Assert the installed branch is identity-keyed and never project-keyed:
   - installed `onOwned` — registers exactly one ownership record per attempt under `ownershipKey(record)`, mutating nothing else.
   - installed `onCleanup` with a **confirming** triple — derives `auditIdentityKey(audit)` as `pid + ':' + processStartTime + ':' + port` from the audit itself, deletes exactly that record under the generation guard, deletes nothing else, appends the audit to `replacementAttemptAudits`, and calls `recordCleanup` **zero** times.
   - installed `onCleanup` with a **non-confirming** triple — moves exactly that identity into `quarantinedOwnership` with `auditState: 'audited-unconfirmed'`, stores the exact audit under `quarantineCleanups`, appends it to `replacementAttemptAudits`, calls `recordCleanup` zero times, and — **[R5]** in this exact order — assigns that `RuntimeFailure('restart-replacement-unconfirmed')` instance to the restart-owned `replacementBlockReason` and then passes the **same instance** to `phaseController.abort(...)`. **[R5]** Assert the blocking consequence per branch by counted primitive calls rather than by a single claim: on a mid-loop collision the delivered `launchReadyRuntime` loop head at `project-runtime-process.ts:794` throws before the next `ports.acquire()` at `:795`; on a last-attempt collision the `for` loop exhausts and the function throws `address-in-use-exhausted` at `:890`; on a readiness, health, or early-exit failure the `catch` at `:876` runs `input.onCleanup?.(cleanup)` at `:882` and then rethrows the already-typed failure at `:883-888` **without** reading `input.signal.aborted`; and only a non-`RuntimeFailure` error reaches `abortFailure(input.signal)` at `:886`. In all four cases assert zero further `ports.acquire` and zero further `process.launch` calls, and assert that the retained category is taken from `replacementBlockReason` via the settlement reason selector on the two branches that do **not** carry it out of `launchReadyRuntime`.
   Assert with a two-attempt fixture that the first attempt's deletion leaves the second attempt's record intact, and that neither wrapper branch ever deletes by `projectId`. Assert the late branch:
   - `onOwned` late — writes only `quarantinedOwnership` with `auditState: 'unaudited'`, leaves `ownership.size` unchanged, and sets `ownedReported: true`.
   - `onCleanup` late — writes only `quarantineCleanups` under the exact `admissionId + '|' + ownershipKey(record)` key, leaves `lastCleanup(projectId)` returning the current operation's or successor's audit byte-for-byte, and sets `audited-absent` only when the triple confirms and `audited-unconfirmed` otherwise.
   - Both — record `entryMutations: 0`, `currentCleanupMutations: 0`, `eventsEmitted: 0`, zero successor mutations, and zero task-set additions.
   Assert explicitly that a blanket no-op is **not** implemented: a late `onOwned` with an exact identity must leave a quarantine record behind that a later restart or the shutdown sweep can terminate, because silently dropping it would leak the process.
7. **Amended post-settlement rule.** Assert the honest constrained rule mechanically: after settlement, zero entries are installed or mutated, zero successors are mutated, zero current-operation ownership records are registered or removed, `cleanupOutcomes` is unmutated, zero lifecycle events are emitted, and nothing becomes routable — **and** that exact late-resource quarantine and quarantine cleanup did occur where a resource existed. Assert both halves; asserting only the prohibition would pass a leaking implementation.
8. **Detached continuation.** Assert exactly one `void launchPromise.then(...)` observer is attached per abandoned launch, and that the promise is absent from `completionTasks`, `backgroundTasks`, `restartTasks`, and `stopTasks` at every recorded instant. Drive all three continuation outcomes:
   - resolves with a `ReadyRuntime` — the exact identity is quarantined if not already, terminated exactly once with **no** caller signal, its audit recorded as a quarantine cleanup, `lateReplacementSettlements` incremented, and the admission set to `audited-absent` on a confirming triple or `materialized-quarantined` otherwise.
   - rejects with `ownedReported === false` — the admission is set to `absent-confirmed` with `createdProcessCount: 0` and no termination is attempted.
   - rejects with `ownedReported === true` — the admission is set to `audited-absent` only when every quarantined identity carries a confirming exact audit, else `materialized-quarantined`.
   Assert `admission.resolution` settles exactly once on every path.
9. **Scenario 60 — reclamation cardinality.** Drive the detached continuation, a resolving restart's release phase, and a manager shutdown sweep against the same `audited-unconfirmed` quarantine record. Assert exactly one continuation termination, at most one claimed re-attempt, that exactly one claimant wins the compare-and-set to `reclaiming`, `concurrentAttempts: 0`, `reattempts <= reattemptClaims`, and exactly one deletion backed by a confirming triple. Assert the sweep leaves `unaudited` records to their in-flight continuation instead of double-terminating them.
10. **Scenario 55 — bounded shutdown.** With an admission left `launch-pending` by a never-settling launch, call `shutdown()` and assert it resolves; that it awaited `restartTasks` (finite, because each restart carries its own 66,000 ms or 81,000 ms deadline) and **never** the launch promise or `admission.resolution`; `awaitedAbandonedLaunch: false`; `RuntimeShutdownResult` has exactly **3** members; `unresolvedAdmissions` has exactly one entry of `{ projectToken, admissionId, phase: 'launch-pending' }` with opaque values only; and `status` is **`'failed'`**, never `'ok'`. Assert no zero-residual claim, completed-audit claim, or clean-shutdown statement exists for that admission anywhere in the result, events, evidence, or documentation.
11. **Scenario 56 — materialisation after shutdown.** Resolve the abandoned launch after `shutdown()` returned and assert the detached continuation still quarantines and terminates the exact identity, records its audit under the quarantine key, and mutates no entry, ownership record, `cleanupOutcomes`, successor, or event.
12. **[R4] Bound variants.** Assert `requiresQuarantineResolution` is read exactly once per restart, synchronously at acceptance, and is true when the project holds an unresolved admission **or** an uncleared quarantine record; that the release backstop is armed at 5,000 ms when it is false and 20,000 ms when it is true; that the overall deadline is armed at 66,000 ms and 81,000 ms respectively; and that exactly **two** `scheduleDeadline` calls occur per restart in both variants, both cancelled on every exit path including the invariant-error path.
12a. **[R4] Quarantine origin is indistinguishable.** Build two fixtures that reach the same `quarantinedOwnership` state — one from a late continuation, one from an installed non-confirming replacement cleanup — and assert byte-identical record shapes, the same `audited-unconfirmed` state, the same reclamation path, the same deletion cardinality of exactly one, the same gate condition, and the same shutdown-sweep handling. Assert the reclaiming restart terminates each quarantined identity exactly once with no caller signal, and that a reclamation that leaves any of the three absences unconfirmed keeps the record rather than deleting it.
12b. **[R4] Cleanup evidence is not overwritten.** With two collision attempts and one late materialisation on one project, assert `quarantineCleanups` holds one entry per distinct identity key, that no write replaces another's audit, that `lastCleanup(projectId)` returns only the restart body's single project-keyed replacement-phase audit, and that `replacementAttemptAudits` retains one entry per attempt in attempt order.
13. **No hidden machinery.** Assert no timer, interval, background loop, or scheduler retries a quarantined identity on its own — reclamation is observed only inside a restart release phase or a shutdown sweep — and that no admission or quarantine value appears in any route response, lifecycle event payload, public projection, browser notice, or `WORKBENCH_FAILURE_TABLE` row.

### Expected Result
No launch can exist without a manager record admitting it; a retained failure whose admission is unresolved is never classified absent; a retry is always accepted but can never install a successor until its predecessor's admission resolves with proven absence or a confirmed exact audit; late callbacks can only quarantine, never mutate settled or successor state; and shutdown always completes in finite time and reports an unresolved admission honestly rather than claiming a clean result.

### Expected Evidence
Matrix rows **52, 53, 54, 55, 56, 57, 58, 59, 60** and **[R4] 61, 62, 63, 64** with the `admission`, `quarantine`, `lateCallbacks`, `taskSets`, `shutdown`, and **[R4]** `replacementAttempts` row objects; the ordering table proving `pendingAdmissions.set` precedes every `launch` and `admissionResolution` precedes every `gate.passed`; the four-row gate-resolution table with its deletion counts; **[R4]** the five-value prior-resource precedence table, the two-origin quarantine equivalence table, and the per-attempt cleanup-key table showing no overwrite; the reclamation cardinality table with `concurrentAttempts: 0`; the `audit()` snapshot per row carrying `pendingAdmissions`, `quarantinedOwnershipRecords`, `quarantineCleanupRecords`, and `admissionResolutions`; the shutdown result with `status: 'failed'` and its one opaque `unresolvedAdmissions` entry; and the bound-variant arming table showing exactly two `scheduleDeadline` calls per restart.
